// AUTO-GENERATED BY VELD — DO NOT EDIT
package maayn.veld.generated.controllers;

import maayn.veld.generated.models.account.*;
import maayn.veld.generated.errors.*;
import maayn.veld.generated.services.IAccountService;
import java.util.List;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
import java.util.Map;

/** Account service */
@RestController
@RequestMapping("/api/accounts_service/account")
public class AccountController {

    private final IAccountService service;

    public AccountController(IAccountService service) {
        this.service = service;
    }

    /** Open a new bank account */
    @PostMapping
    public ResponseEntity<?> openAccount(@RequestBody OpenAccountRequest body) {
        try {
            AccountResponse result = service.openAccount(body);
            return ResponseEntity.status(HttpStatus.CREATED).body(result);
        } catch (ApiException e) {
            return ResponseEntity.status(e.getStatus()).body(e.toErrorResponse());
        } catch (Exception e) {
            return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(ApiException.toErrorResponse(e));
        }
    }

    /** Get account details with balance */
    @GetMapping("/{accountId}")
    public ResponseEntity<?> getAccount(@PathVariable("accountId") String accountId) {
        try {
            AccountResponse result = service.getAccount(accountId);
            return ResponseEntity.ok(result);
        } catch (ApiException e) {
            return ResponseEntity.status(e.getStatus()).body(e.toErrorResponse());
        } catch (Exception e) {
            return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(ApiException.toErrorResponse(e));
        }
    }

    /** List all accounts for a customer */
    @GetMapping("/customer/{customerId}")
    public ResponseEntity<?> listCustomerAccounts(@PathVariable("customerId") String customerId) {
        try {
            List<AccountResponse> result = service.listCustomerAccounts(customerId);
            return ResponseEntity.ok(result);
        } catch (ApiException e) {
            return ResponseEntity.status(e.getStatus()).body(e.toErrorResponse());
        } catch (Exception e) {
            return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(ApiException.toErrorResponse(e));
        }
    }

    /** Close an account */
    @PostMapping("/{accountId}/close")
    public ResponseEntity<?> closeAccount(@PathVariable("accountId") String accountId, @RequestBody CloseAccountRequest body) {
        try {
            AccountResponse result = service.closeAccount(accountId, body);
            return ResponseEntity.status(HttpStatus.CREATED).body(result);
        } catch (ApiException e) {
            return ResponseEntity.status(e.getStatus()).body(e.toErrorResponse());
        } catch (Exception e) {
            return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(ApiException.toErrorResponse(e));
        }
    }

    /** Update account status (freeze/activate) */
    @PutMapping("/{accountId}/status")
    public ResponseEntity<?> updateAccountStatus(@PathVariable("accountId") String accountId, @RequestBody UpdateAccountStatusRequest body) {
        try {
            AccountResponse result = service.updateAccountStatus(accountId, body);
            return ResponseEntity.ok(result);
        } catch (ApiException e) {
            return ResponseEntity.status(e.getStatus()).body(e.toErrorResponse());
        } catch (Exception e) {
            return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(ApiException.toErrorResponse(e));
        }
    }

    /** Check if account exists */
    @GetMapping("/{accountId}/exists")
    public ResponseEntity<?> doesAccountExist(@PathVariable("accountId") String accountId) {
        try {
            Boolean result = service.doesAccountExist(accountId);
            return ResponseEntity.ok(result);
        } catch (ApiException e) {
            return ResponseEntity.status(e.getStatus()).body(e.toErrorResponse());
        } catch (Exception e) {
            return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(ApiException.toErrorResponse(e));
        }
    }
}
