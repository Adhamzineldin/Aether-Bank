// AUTO-GENERATED BY VELD — DO NOT EDIT
package maayn.veld.generated.controllers;

import maayn.veld.generated.models.authentication.*;
import maayn.veld.generated.errors.*;
import maayn.veld.generated.services.IAuthenticationService;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
import java.util.Map;

/** Handles authentication operations */
@RestController
@RequestMapping("/api/auth")
public class AuthenticationController {

    private final IAuthenticationService service;

    public AuthenticationController(IAuthenticationService service) {
        this.service = service;
    }

    @PostMapping("/login")
    public ResponseEntity<?> login(@RequestBody LoginRequest body) {
        try {
            JwtResponse result = service.login(body);
            return ResponseEntity.status(HttpStatus.CREATED).body(result);
        } catch (ApiException e) {
            return ResponseEntity.status(e.getStatus()).body(e.toErrorResponse());
        } catch (Exception e) {
            return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(ApiException.toErrorResponse(e));
        }
    }

    @PostMapping("/register")
    public ResponseEntity<?> register(@RequestBody RegisterRequest body) {
        try {
            UserResponse result = service.register(body);
            return ResponseEntity.status(HttpStatus.CREATED).body(result);
        } catch (ApiException e) {
            return ResponseEntity.status(e.getStatus()).body(e.toErrorResponse());
        } catch (Exception e) {
            return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(ApiException.toErrorResponse(e));
        }
    }

    @PostMapping("/logout")
    public ResponseEntity<?> logout() {
        try {
            GenericResponse result = service.logout();
            return ResponseEntity.status(HttpStatus.CREATED).body(result);
        } catch (ApiException e) {
            return ResponseEntity.status(e.getStatus()).body(e.toErrorResponse());
        } catch (Exception e) {
            return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(ApiException.toErrorResponse(e));
        }
    }
}
