// AUTO-GENERATED BY VELD — DO NOT EDIT
package maayn.veld.generated.errors;

/**
 * Error factories for the Transaction module — mirrors the TypeScript transactionErrors pattern.
 * <pre>{@code
 * throw TransactionErrors.transfer.insufficientFunds("message");
 * }</pre>
 */
public final class TransactionErrors {
    private TransactionErrors() {}

    /** Error factory for the transfer action. */
    public static final TransferErrors transfer = new TransferErrors();

    /** Per-error factories and code constants for transfer.
     * All members are static — access via singleton (TransactionErrors.transfer.method())
     * or directly via class (TransferErrors.method()). */
    public static final class TransferErrors {
        private TransferErrors() {}

        /** Error code — mirrors TS transactionErrors.transfer.insufficientFunds.code */
        public static final String INSUFFICIENT_FUNDS_CODE = "TRANSFER_INSUFFICIENT_FUNDS";
        public static final int INSUFFICIENT_FUNDS_STATUS = 403;

        /** Create a InsufficientFunds error (403). */
        public static TransferException insufficientFunds(String message) {
            return new TransferException("TRANSFER_INSUFFICIENT_FUNDS", 403, message != null ? message : "InsufficientFunds");
        }

        public static TransferException insufficientFunds() { return insufficientFunds(null); }

        /** Error code — mirrors TS transactionErrors.transfer.invalidAccount.code */
        public static final String INVALID_ACCOUNT_CODE = "TRANSFER_INVALID_ACCOUNT";
        public static final int INVALID_ACCOUNT_STATUS = 404;

        /** Create a InvalidAccount error (404). */
        public static TransferException invalidAccount(String message) {
            return new TransferException("TRANSFER_INVALID_ACCOUNT", 404, message != null ? message : "InvalidAccount");
        }

        public static TransferException invalidAccount() { return invalidAccount(null); }

        /** Error code — mirrors TS transactionErrors.transfer.unauthorized.code */
        public static final String UNAUTHORIZED_CODE = "TRANSFER_UNAUTHORIZED";
        public static final int UNAUTHORIZED_STATUS = 401;

        /** Create a Unauthorized error (401). */
        public static TransferException unauthorized(String message) {
            return new TransferException("TRANSFER_UNAUTHORIZED", 401, message != null ? message : "Unauthorized");
        }

        public static TransferException unauthorized() { return unauthorized(null); }

        /** Error code — mirrors TS transactionErrors.transfer.invalidAmount.code */
        public static final String INVALID_AMOUNT_CODE = "TRANSFER_INVALID_AMOUNT";
        public static final int INVALID_AMOUNT_STATUS = 400;

        /** Create a InvalidAmount error (400). */
        public static TransferException invalidAmount(String message) {
            return new TransferException("TRANSFER_INVALID_AMOUNT", 400, message != null ? message : "InvalidAmount");
        }

        public static TransferException invalidAmount() { return invalidAmount(null); }

        /** Error code — mirrors TS transactionErrors.transfer.invalidTarget.code */
        public static final String INVALID_TARGET_CODE = "TRANSFER_INVALID_TARGET";
        public static final int INVALID_TARGET_STATUS = 400;

        /** Create a invalidTarget error (400). */
        public static TransferException invalidTarget(String message) {
            return new TransferException("TRANSFER_INVALID_TARGET", 400, message != null ? message : "invalidTarget");
        }

        public static TransferException invalidTarget() { return invalidTarget(null); }

    }

}
