// Code generated by Veld — DO NOT EDIT.
package maayn.veld.generated.sdk.account.errors;

/**
 * Error constants and matchers for the Account module.
 * Mirrors the server-side {@code AccountErrors} pattern.
 * <pre>{@code
 * } catch (SdkApiError e) {
 *     if (AccountErrors.openAccount.isCustomerNotFound(e)) { ... }
 * }
 * }</pre>
 */
public final class AccountErrors {
    private AccountErrors() {}

    /** Error constants and matchers for the openAccount action. */
    public static final OpenAccountErrors openAccount = new OpenAccountErrors();

    /** Per-error constants and matchers for openAccount.
     * Constants match server-side {@code AccountErrors.openAccount.*_CODE} exactly. */
    public static final class OpenAccountErrors {
        private OpenAccountErrors() {}

        /** Matches server-side accountErrors.openAccount.CUSTOMER_NOT_FOUND_CODE = "OPEN_ACCOUNT_CUSTOMER_NOT_FOUND" */
        public static final String CUSTOMER_NOT_FOUND_CODE = "OPEN_ACCOUNT_CUSTOMER_NOT_FOUND";
        public static final int CUSTOMER_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a CustomerNotFound error (404). */
        public static boolean isCustomerNotFound(SdkApiError e) {
            return CUSTOMER_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is CustomerNotFound. */
        public boolean customerNotFound(SdkApiError e) { return isCustomerNotFound(e); }

        /** Matches server-side accountErrors.openAccount.INVALID_CURRENCY_CODE = "OPEN_ACCOUNT_INVALID_CURRENCY" */
        public static final String INVALID_CURRENCY_CODE = "OPEN_ACCOUNT_INVALID_CURRENCY";
        public static final int INVALID_CURRENCY_STATUS = 400;

        /** Returns true if the SdkApiError is a InvalidCurrency error (400). */
        public static boolean isInvalidCurrency(SdkApiError e) {
            return INVALID_CURRENCY_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InvalidCurrency. */
        public boolean invalidCurrency(SdkApiError e) { return isInvalidCurrency(e); }

        /** Matches server-side accountErrors.openAccount.INVALID_ACCOUNT_TYPE_CODE = "OPEN_ACCOUNT_INVALID_ACCOUNT_TYPE" */
        public static final String INVALID_ACCOUNT_TYPE_CODE = "OPEN_ACCOUNT_INVALID_ACCOUNT_TYPE";
        public static final int INVALID_ACCOUNT_TYPE_STATUS = 400;

        /** Returns true if the SdkApiError is a InvalidAccountType error (400). */
        public static boolean isInvalidAccountType(SdkApiError e) {
            return INVALID_ACCOUNT_TYPE_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InvalidAccountType. */
        public boolean invalidAccountType(SdkApiError e) { return isInvalidAccountType(e); }

    }

    /** Error constants and matchers for the getAccount action. */
    public static final GetAccountErrors getAccount = new GetAccountErrors();

    /** Per-error constants and matchers for getAccount.
     * Constants match server-side {@code AccountErrors.getAccount.*_CODE} exactly. */
    public static final class GetAccountErrors {
        private GetAccountErrors() {}

        /** Matches server-side accountErrors.getAccount.ACCOUNT_NOT_FOUND_CODE = "GET_ACCOUNT_ACCOUNT_NOT_FOUND" */
        public static final String ACCOUNT_NOT_FOUND_CODE = "GET_ACCOUNT_ACCOUNT_NOT_FOUND";
        public static final int ACCOUNT_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a AccountNotFound error (404). */
        public static boolean isAccountNotFound(SdkApiError e) {
            return ACCOUNT_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is AccountNotFound. */
        public boolean accountNotFound(SdkApiError e) { return isAccountNotFound(e); }

    }

    /** Error constants and matchers for the listCustomerAccounts action. */
    public static final ListCustomerAccountsErrors listCustomerAccounts = new ListCustomerAccountsErrors();

    /** Per-error constants and matchers for listCustomerAccounts.
     * Constants match server-side {@code AccountErrors.listCustomerAccounts.*_CODE} exactly. */
    public static final class ListCustomerAccountsErrors {
        private ListCustomerAccountsErrors() {}

        /** Matches server-side accountErrors.listCustomerAccounts.CUSTOMER_NOT_FOUND_CODE = "LIST_CUSTOMER_ACCOUNTS_CUSTOMER_NOT_FOUND" */
        public static final String CUSTOMER_NOT_FOUND_CODE = "LIST_CUSTOMER_ACCOUNTS_CUSTOMER_NOT_FOUND";
        public static final int CUSTOMER_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a CustomerNotFound error (404). */
        public static boolean isCustomerNotFound(SdkApiError e) {
            return CUSTOMER_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is CustomerNotFound. */
        public boolean customerNotFound(SdkApiError e) { return isCustomerNotFound(e); }

    }

    /** Error constants and matchers for the closeAccount action. */
    public static final CloseAccountErrors closeAccount = new CloseAccountErrors();

    /** Per-error constants and matchers for closeAccount.
     * Constants match server-side {@code AccountErrors.closeAccount.*_CODE} exactly. */
    public static final class CloseAccountErrors {
        private CloseAccountErrors() {}

        /** Matches server-side accountErrors.closeAccount.ACCOUNT_NOT_FOUND_CODE = "CLOSE_ACCOUNT_ACCOUNT_NOT_FOUND" */
        public static final String ACCOUNT_NOT_FOUND_CODE = "CLOSE_ACCOUNT_ACCOUNT_NOT_FOUND";
        public static final int ACCOUNT_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a AccountNotFound error (404). */
        public static boolean isAccountNotFound(SdkApiError e) {
            return ACCOUNT_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is AccountNotFound. */
        public boolean accountNotFound(SdkApiError e) { return isAccountNotFound(e); }

        /** Matches server-side accountErrors.closeAccount.ACCOUNT_HAS_BALANCE_CODE = "CLOSE_ACCOUNT_ACCOUNT_HAS_BALANCE" */
        public static final String ACCOUNT_HAS_BALANCE_CODE = "CLOSE_ACCOUNT_ACCOUNT_HAS_BALANCE";
        public static final int ACCOUNT_HAS_BALANCE_STATUS = 400;

        /** Returns true if the SdkApiError is a AccountHasBalance error (400). */
        public static boolean isAccountHasBalance(SdkApiError e) {
            return ACCOUNT_HAS_BALANCE_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is AccountHasBalance. */
        public boolean accountHasBalance(SdkApiError e) { return isAccountHasBalance(e); }

        /** Matches server-side accountErrors.closeAccount.ACCOUNT_ALREADY_CLOSED_CODE = "CLOSE_ACCOUNT_ACCOUNT_ALREADY_CLOSED" */
        public static final String ACCOUNT_ALREADY_CLOSED_CODE = "CLOSE_ACCOUNT_ACCOUNT_ALREADY_CLOSED";
        public static final int ACCOUNT_ALREADY_CLOSED_STATUS = 400;

        /** Returns true if the SdkApiError is a AccountAlreadyClosed error (400). */
        public static boolean isAccountAlreadyClosed(SdkApiError e) {
            return ACCOUNT_ALREADY_CLOSED_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is AccountAlreadyClosed. */
        public boolean accountAlreadyClosed(SdkApiError e) { return isAccountAlreadyClosed(e); }

    }

    /** Error constants and matchers for the updateAccountStatus action. */
    public static final UpdateAccountStatusErrors updateAccountStatus = new UpdateAccountStatusErrors();

    /** Per-error constants and matchers for updateAccountStatus.
     * Constants match server-side {@code AccountErrors.updateAccountStatus.*_CODE} exactly. */
    public static final class UpdateAccountStatusErrors {
        private UpdateAccountStatusErrors() {}

        /** Matches server-side accountErrors.updateAccountStatus.ACCOUNT_NOT_FOUND_CODE = "UPDATE_ACCOUNT_STATUS_ACCOUNT_NOT_FOUND" */
        public static final String ACCOUNT_NOT_FOUND_CODE = "UPDATE_ACCOUNT_STATUS_ACCOUNT_NOT_FOUND";
        public static final int ACCOUNT_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a AccountNotFound error (404). */
        public static boolean isAccountNotFound(SdkApiError e) {
            return ACCOUNT_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is AccountNotFound. */
        public boolean accountNotFound(SdkApiError e) { return isAccountNotFound(e); }

        /** Matches server-side accountErrors.updateAccountStatus.INVALID_STATUS_TRANSITION_CODE = "UPDATE_ACCOUNT_STATUS_INVALID_STATUS_TRANSITION" */
        public static final String INVALID_STATUS_TRANSITION_CODE = "UPDATE_ACCOUNT_STATUS_INVALID_STATUS_TRANSITION";
        public static final int INVALID_STATUS_TRANSITION_STATUS = 400;

        /** Returns true if the SdkApiError is a InvalidStatusTransition error (400). */
        public static boolean isInvalidStatusTransition(SdkApiError e) {
            return INVALID_STATUS_TRANSITION_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InvalidStatusTransition. */
        public boolean invalidStatusTransition(SdkApiError e) { return isInvalidStatusTransition(e); }

    }

}
