server:
  port: 3030

spring:
  application:
    name: workflow-notification-service

  data:
    mongodb:
#      Will check the deprecated method
      uri: mongodb://localhost:27030/workflow_notification_service
      auto-index-creation: true

#  Will check where to configure rabbitmq

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enabe: true


eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
#      How other services will find me (hostname or ip) // check this one Adham
#  instance:
#    prefer-ip-address: true

logging:
  level:
    com.maayn.notificationservice: DEBUG
    org.springframework.data.mongodb: DEBUG