// Code generated by Veld — DO NOT EDIT.
package maayn.veld.generated.sdk.account;

import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.util.Map;
import java.util.HashMap;
import java.util.List;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.SerializationFeature;
import com.fasterxml.jackson.datatype.jsr310.JavaTimeModule;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;
import maayn.veld.generated.sdk.account.errors.SdkApiError;
import maayn.veld.generated.sdk.account.models.account.*;
import maayn.veld.generated.sdk.account.models.shared.*;

@Component
public class AccountClient {
    private final String base;
    private final HttpClient http;
    private final Map<String, String> headers;
    private final ObjectMapper mapper = new ObjectMapper()
        .registerModule(new JavaTimeModule())
        .disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);

    /** Actions for the Account module. Usage: client.account.actionName(...) */
    public final AccountActions account = new AccountActions();

    /**
     * Spring-managed constructor.
     * Configure via {@code veld.sdk.account.base-url} in application.properties,
     * or set the {@code VELD_ACCOUNT_URL} environment variable.
     */
    @Autowired
    public AccountClient(@Value("${veld.sdk.account.base-url:${VELD_ACCOUNT_URL:http://account-service:3003}}") String baseUrl) {
        this.base = baseUrl;
        this.http = HttpClient.newHttpClient();
        this.headers = new HashMap<>();
    }

    /** Manual constructor for non-Spring usage or testing. */
    public AccountClient(String baseUrl, Map<String, String> headers) {
        this.base = baseUrl != null ? baseUrl : System.getenv("VELD_ACCOUNT_URL") != null ? System.getenv("VELD_ACCOUNT_URL") : "http://account-service:3003";
        this.http = HttpClient.newHttpClient();
        this.headers = headers != null ? headers : new HashMap<>();
    }

    String request(String method, String path, String body) throws Exception {
        HttpRequest.Builder builder = HttpRequest.newBuilder(URI.create(this.base + path))
            .header("Content-Type", "application/json");
        this.headers.forEach(builder::header);
        if (body != null) { builder.method(method, HttpRequest.BodyPublishers.ofString(body)); }
        else { builder.method(method, HttpRequest.BodyPublishers.noBody()); }
        HttpResponse<String> res = http.send(builder.build(), HttpResponse.BodyHandlers.ofString());
        if (res.statusCode() >= 400) {
            String errBody = res.body();
            String code = extractJsonString(errBody, "code");
            String msg  = extractJsonString(errBody, "error");
            if (code == null) code = "UNKNOWN_ERROR";
            if (msg  == null) msg  = errBody;
            throw new SdkApiError(code, res.statusCode(), msg);
        }
        return res.body();
    }

    ObjectMapper mapper() { return mapper; }

    static String extractJsonString(String json, String key) {
        if (json == null) return null;
        String search = "\"" + key + "\"";
        int idx = json.indexOf(search);
        if (idx < 0) return null;
        int colon = json.indexOf(':', idx + search.length());
        if (colon < 0) return null;
        int start = json.indexOf('"', colon + 1);
        if (start < 0) return null;
        int end = json.indexOf('"', start + 1);
        if (end < 0) return null;
        return json.substring(start + 1, end);
    }

    /** Actions for the Account module. */
    public final class AccountActions {
        private AccountActions() {}

        /** POST /api/accounts_service/account/ */
        public AccountResponse openAccount(OpenAccountRequest input) throws Exception {
            String body = mapper().writeValueAsString(input);
            String raw = request("POST", "/api/accounts_service/account/", body);
            return mapper().readValue(raw, AccountResponse.class);
        }

        /** GET /api/accounts_service/account/:accountId */
        public AccountResponse getAccount(String accountId) throws Exception {
            String raw = request("GET", "/api/accounts_service/account/" + accountId, null);
            return mapper().readValue(raw, AccountResponse.class);
        }

        /** GET /api/accounts_service/account/customer/:customerId */
        public List<AccountResponse> listCustomerAccounts(String customerId) throws Exception {
            String raw = request("GET", "/api/accounts_service/account/customer/" + customerId, null);
            return mapper().readValue(raw, mapper().getTypeFactory().constructCollectionType(java.util.List.class, AccountResponse.class));
        }

        /** POST /api/accounts_service/account/:accountId/close */
        public AccountResponse closeAccount(String accountId, CloseAccountRequest input) throws Exception {
            String body = mapper().writeValueAsString(input);
            String raw = request("POST", "/api/accounts_service/account/" + accountId + "/close", body);
            return mapper().readValue(raw, AccountResponse.class);
        }

        /** PUT /api/accounts_service/account/:accountId/status */
        public AccountResponse updateAccountStatus(String accountId, UpdateAccountStatusRequest input) throws Exception {
            String body = mapper().writeValueAsString(input);
            String raw = request("PUT", "/api/accounts_service/account/" + accountId + "/status", body);
            return mapper().readValue(raw, AccountResponse.class);
        }

        /** GET /api/accounts_service/account/:accountId/exists */
        public Boolean doesAccountExist(String accountId) throws Exception {
            String raw = request("GET", "/api/accounts_service/account/" + accountId + "/exists", null);
            return mapper().readValue(raw, Boolean.class);
        }
    }

}
