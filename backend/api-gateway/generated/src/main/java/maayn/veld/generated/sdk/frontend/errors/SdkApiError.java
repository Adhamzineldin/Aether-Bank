// Code generated by Veld — DO NOT EDIT.
package maayn.veld.generated.sdk.frontend.errors;

/**
 * Typed exception thrown by the SDK client when a remote service returns an HTTP error.
 * Mirrors {@code ApiException} on the server — same check patterns:
 * <pre>{@code
 * try {
 *     iamClient.login(req);
 * } catch (SdkApiError e) {
 *     if (IamErrors.login.isUnauthorized(e)) { ... }
 *     if (IamErrors.Login.UNAUTHORIZED_CODE.equals(e.getCode())) { ... }
 *     if (e.isCode("LOGIN_UNAUTHORIZED")) { ... }
 *     if (e.isStatus(401)) { ... }
 * }
 * }</pre>
 */
public class SdkApiError extends RuntimeException {
    private final String code;
    private final int status;

    public SdkApiError(String code, int status, String message) {
        super(message);
        this.code = code;
        this.status = status;
    }

    /** Machine-readable error code from the contract, e.g. "LOGIN_UNAUTHORIZED". */
    public String getCode() { return code; }

    /** HTTP status code, e.g. 401. */
    public int getStatus() { return status; }

    /** Returns true if this error's code matches. */
    public boolean isCode(String errorCode) { return this.code.equals(errorCode); }

    /** Returns true if this error's HTTP status matches. */
    public boolean isStatus(int httpStatus) { return this.status == httpStatus; }
}
