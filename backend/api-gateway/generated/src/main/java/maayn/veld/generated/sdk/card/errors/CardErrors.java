// Code generated by Veld — DO NOT EDIT.
package maayn.veld.generated.sdk.card.errors;

/**
 * Error constants and matchers for the Card module.
 * Mirrors the server-side {@code CardErrors} pattern.
 * <pre>{@code
 * } catch (SdkApiError e) {
 *     if (CardErrors.getCardDetails.isNotFound(e)) { ... }
 * }
 * }</pre>
 */
public final class CardErrors {
    private CardErrors() {}

    /** Error constants and matchers for the getCardDetails action. */
    public static final GetCardDetailsErrors getCardDetails = new GetCardDetailsErrors();

    /** Per-error constants and matchers for getCardDetails.
     * Constants match server-side {@code CardErrors.getCardDetails.*_CODE} exactly. */
    public static final class GetCardDetailsErrors {
        private GetCardDetailsErrors() {}

        /** Matches server-side cardErrors.getCardDetails.NOT_FOUND_CODE = "GET_CARD_DETAILS_NOT_FOUND" */
        public static final String NOT_FOUND_CODE = "GET_CARD_DETAILS_NOT_FOUND";
        public static final int NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a NotFound error (404). */
        public static boolean isNotFound(SdkApiError e) {
            return NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is NotFound. */
        public boolean notFound(SdkApiError e) { return isNotFound(e); }

        /** Matches server-side cardErrors.getCardDetails.UNAUTHORIZED_CODE = "GET_CARD_DETAILS_UNAUTHORIZED" */
        public static final String UNAUTHORIZED_CODE = "GET_CARD_DETAILS_UNAUTHORIZED";
        public static final int UNAUTHORIZED_STATUS = 401;

        /** Returns true if the SdkApiError is a Unauthorized error (401). */
        public static boolean isUnauthorized(SdkApiError e) {
            return UNAUTHORIZED_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is Unauthorized. */
        public boolean unauthorized(SdkApiError e) { return isUnauthorized(e); }

    }

    /** Error constants and matchers for the revealPan action. */
    public static final RevealPanErrors revealPan = new RevealPanErrors();

    /** Per-error constants and matchers for revealPan.
     * Constants match server-side {@code CardErrors.revealPan.*_CODE} exactly. */
    public static final class RevealPanErrors {
        private RevealPanErrors() {}

        /** Matches server-side cardErrors.revealPan.NOT_FOUND_CODE = "REVEAL_PAN_NOT_FOUND" */
        public static final String NOT_FOUND_CODE = "REVEAL_PAN_NOT_FOUND";
        public static final int NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a NotFound error (404). */
        public static boolean isNotFound(SdkApiError e) {
            return NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is NotFound. */
        public boolean notFound(SdkApiError e) { return isNotFound(e); }

        /** Matches server-side cardErrors.revealPan.UNAUTHORIZED_CODE = "REVEAL_PAN_UNAUTHORIZED" */
        public static final String UNAUTHORIZED_CODE = "REVEAL_PAN_UNAUTHORIZED";
        public static final int UNAUTHORIZED_STATUS = 401;

        /** Returns true if the SdkApiError is a Unauthorized error (401). */
        public static boolean isUnauthorized(SdkApiError e) {
            return UNAUTHORIZED_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is Unauthorized. */
        public boolean unauthorized(SdkApiError e) { return isUnauthorized(e); }

    }

    /** Error constants and matchers for the processMerchantPayment action. */
    public static final ProcessMerchantPaymentErrors processMerchantPayment = new ProcessMerchantPaymentErrors();

    /** Per-error constants and matchers for processMerchantPayment.
     * Constants match server-side {@code CardErrors.processMerchantPayment.*_CODE} exactly. */
    public static final class ProcessMerchantPaymentErrors {
        private ProcessMerchantPaymentErrors() {}

        /** Matches server-side cardErrors.processMerchantPayment.INVALID_CARD_TOKEN_CODE = "PROCESS_MERCHANT_PAYMENT_INVALID_CARD_TOKEN" */
        public static final String INVALID_CARD_TOKEN_CODE = "PROCESS_MERCHANT_PAYMENT_INVALID_CARD_TOKEN";
        public static final int INVALID_CARD_TOKEN_STATUS = 404;

        /** Returns true if the SdkApiError is a InvalidCardToken error (404). */
        public static boolean isInvalidCardToken(SdkApiError e) {
            return INVALID_CARD_TOKEN_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InvalidCardToken. */
        public boolean invalidCardToken(SdkApiError e) { return isInvalidCardToken(e); }

        /** Matches server-side cardErrors.processMerchantPayment.CARD_NOT_ACTIVE_CODE = "PROCESS_MERCHANT_PAYMENT_CARD_NOT_ACTIVE" */
        public static final String CARD_NOT_ACTIVE_CODE = "PROCESS_MERCHANT_PAYMENT_CARD_NOT_ACTIVE";
        public static final int CARD_NOT_ACTIVE_STATUS = 403;

        /** Returns true if the SdkApiError is a CardNotActive error (403). */
        public static boolean isCardNotActive(SdkApiError e) {
            return CARD_NOT_ACTIVE_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is CardNotActive. */
        public boolean cardNotActive(SdkApiError e) { return isCardNotActive(e); }

        /** Matches server-side cardErrors.processMerchantPayment.CARD_EXPIRED_CODE = "PROCESS_MERCHANT_PAYMENT_CARD_EXPIRED" */
        public static final String CARD_EXPIRED_CODE = "PROCESS_MERCHANT_PAYMENT_CARD_EXPIRED";
        public static final int CARD_EXPIRED_STATUS = 403;

        /** Returns true if the SdkApiError is a CardExpired error (403). */
        public static boolean isCardExpired(SdkApiError e) {
            return CARD_EXPIRED_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is CardExpired. */
        public boolean cardExpired(SdkApiError e) { return isCardExpired(e); }

        /** Matches server-side cardErrors.processMerchantPayment.INSUFFICIENT_CREDIT_CODE = "PROCESS_MERCHANT_PAYMENT_INSUFFICIENT_CREDIT" */
        public static final String INSUFFICIENT_CREDIT_CODE = "PROCESS_MERCHANT_PAYMENT_INSUFFICIENT_CREDIT";
        public static final int INSUFFICIENT_CREDIT_STATUS = 422;

        /** Returns true if the SdkApiError is a InsufficientCredit error (422). */
        public static boolean isInsufficientCredit(SdkApiError e) {
            return INSUFFICIENT_CREDIT_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InsufficientCredit. */
        public boolean insufficientCredit(SdkApiError e) { return isInsufficientCredit(e); }

        /** Matches server-side cardErrors.processMerchantPayment.INVALID_CURRENCY_CODE = "PROCESS_MERCHANT_PAYMENT_INVALID_CURRENCY" */
        public static final String INVALID_CURRENCY_CODE = "PROCESS_MERCHANT_PAYMENT_INVALID_CURRENCY";
        public static final int INVALID_CURRENCY_STATUS = 400;

        /** Returns true if the SdkApiError is a InvalidCurrency error (400). */
        public static boolean isInvalidCurrency(SdkApiError e) {
            return INVALID_CURRENCY_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InvalidCurrency. */
        public boolean invalidCurrency(SdkApiError e) { return isInvalidCurrency(e); }

        /** Matches server-side cardErrors.processMerchantPayment.INVALID_AMOUNT_CODE = "PROCESS_MERCHANT_PAYMENT_INVALID_AMOUNT" */
        public static final String INVALID_AMOUNT_CODE = "PROCESS_MERCHANT_PAYMENT_INVALID_AMOUNT";
        public static final int INVALID_AMOUNT_STATUS = 400;

        /** Returns true if the SdkApiError is a InvalidAmount error (400). */
        public static boolean isInvalidAmount(SdkApiError e) {
            return INVALID_AMOUNT_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InvalidAmount. */
        public boolean invalidAmount(SdkApiError e) { return isInvalidAmount(e); }

        /** Matches server-side cardErrors.processMerchantPayment.UNAUTHORIZED_CODE = "PROCESS_MERCHANT_PAYMENT_UNAUTHORIZED" */
        public static final String UNAUTHORIZED_CODE = "PROCESS_MERCHANT_PAYMENT_UNAUTHORIZED";
        public static final int UNAUTHORIZED_STATUS = 401;

        /** Returns true if the SdkApiError is a Unauthorized error (401). */
        public static boolean isUnauthorized(SdkApiError e) {
            return UNAUTHORIZED_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is Unauthorized. */
        public boolean unauthorized(SdkApiError e) { return isUnauthorized(e); }

    }

    /** Error constants and matchers for the refundTransaction action. */
    public static final RefundTransactionErrors refundTransaction = new RefundTransactionErrors();

    /** Per-error constants and matchers for refundTransaction.
     * Constants match server-side {@code CardErrors.refundTransaction.*_CODE} exactly. */
    public static final class RefundTransactionErrors {
        private RefundTransactionErrors() {}

        /** Matches server-side cardErrors.refundTransaction.TRANSACTION_NOT_FOUND_CODE = "REFUND_TRANSACTION_TRANSACTION_NOT_FOUND" */
        public static final String TRANSACTION_NOT_FOUND_CODE = "REFUND_TRANSACTION_TRANSACTION_NOT_FOUND";
        public static final int TRANSACTION_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a TransactionNotFound error (404). */
        public static boolean isTransactionNotFound(SdkApiError e) {
            return TRANSACTION_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is TransactionNotFound. */
        public boolean transactionNotFound(SdkApiError e) { return isTransactionNotFound(e); }

        /** Matches server-side cardErrors.refundTransaction.REFUND_NOT_ALLOWED_CODE = "REFUND_TRANSACTION_REFUND_NOT_ALLOWED" */
        public static final String REFUND_NOT_ALLOWED_CODE = "REFUND_TRANSACTION_REFUND_NOT_ALLOWED";
        public static final int REFUND_NOT_ALLOWED_STATUS = 409;

        /** Returns true if the SdkApiError is a RefundNotAllowed error (409). */
        public static boolean isRefundNotAllowed(SdkApiError e) {
            return REFUND_NOT_ALLOWED_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is RefundNotAllowed. */
        public boolean refundNotAllowed(SdkApiError e) { return isRefundNotAllowed(e); }

        /** Matches server-side cardErrors.refundTransaction.INVALID_AMOUNT_CODE = "REFUND_TRANSACTION_INVALID_AMOUNT" */
        public static final String INVALID_AMOUNT_CODE = "REFUND_TRANSACTION_INVALID_AMOUNT";
        public static final int INVALID_AMOUNT_STATUS = 400;

        /** Returns true if the SdkApiError is a InvalidAmount error (400). */
        public static boolean isInvalidAmount(SdkApiError e) {
            return INVALID_AMOUNT_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InvalidAmount. */
        public boolean invalidAmount(SdkApiError e) { return isInvalidAmount(e); }

        /** Matches server-side cardErrors.refundTransaction.UNAUTHORIZED_CODE = "REFUND_TRANSACTION_UNAUTHORIZED" */
        public static final String UNAUTHORIZED_CODE = "REFUND_TRANSACTION_UNAUTHORIZED";
        public static final int UNAUTHORIZED_STATUS = 401;

        /** Returns true if the SdkApiError is a Unauthorized error (401). */
        public static boolean isUnauthorized(SdkApiError e) {
            return UNAUTHORIZED_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is Unauthorized. */
        public boolean unauthorized(SdkApiError e) { return isUnauthorized(e); }

    }

    /** Error constants and matchers for the voidTransaction action. */
    public static final VoidTransactionErrors voidTransaction = new VoidTransactionErrors();

    /** Per-error constants and matchers for voidTransaction.
     * Constants match server-side {@code CardErrors.voidTransaction.*_CODE} exactly. */
    public static final class VoidTransactionErrors {
        private VoidTransactionErrors() {}

        /** Matches server-side cardErrors.voidTransaction.TRANSACTION_NOT_FOUND_CODE = "VOID_TRANSACTION_TRANSACTION_NOT_FOUND" */
        public static final String TRANSACTION_NOT_FOUND_CODE = "VOID_TRANSACTION_TRANSACTION_NOT_FOUND";
        public static final int TRANSACTION_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a TransactionNotFound error (404). */
        public static boolean isTransactionNotFound(SdkApiError e) {
            return TRANSACTION_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is TransactionNotFound. */
        public boolean transactionNotFound(SdkApiError e) { return isTransactionNotFound(e); }

        /** Matches server-side cardErrors.voidTransaction.VOID_NOT_ALLOWED_CODE = "VOID_TRANSACTION_VOID_NOT_ALLOWED" */
        public static final String VOID_NOT_ALLOWED_CODE = "VOID_TRANSACTION_VOID_NOT_ALLOWED";
        public static final int VOID_NOT_ALLOWED_STATUS = 409;

        /** Returns true if the SdkApiError is a VoidNotAllowed error (409). */
        public static boolean isVoidNotAllowed(SdkApiError e) {
            return VOID_NOT_ALLOWED_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is VoidNotAllowed. */
        public boolean voidNotAllowed(SdkApiError e) { return isVoidNotAllowed(e); }

        /** Matches server-side cardErrors.voidTransaction.UNAUTHORIZED_CODE = "VOID_TRANSACTION_UNAUTHORIZED" */
        public static final String UNAUTHORIZED_CODE = "VOID_TRANSACTION_UNAUTHORIZED";
        public static final int UNAUTHORIZED_STATUS = 401;

        /** Returns true if the SdkApiError is a Unauthorized error (401). */
        public static boolean isUnauthorized(SdkApiError e) {
            return UNAUTHORIZED_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is Unauthorized. */
        public boolean unauthorized(SdkApiError e) { return isUnauthorized(e); }

    }

    /** Error constants and matchers for the getCardTransactions action. */
    public static final GetCardTransactionsErrors getCardTransactions = new GetCardTransactionsErrors();

    /** Per-error constants and matchers for getCardTransactions.
     * Constants match server-side {@code CardErrors.getCardTransactions.*_CODE} exactly. */
    public static final class GetCardTransactionsErrors {
        private GetCardTransactionsErrors() {}

        /** Matches server-side cardErrors.getCardTransactions.NOT_FOUND_CODE = "GET_CARD_TRANSACTIONS_NOT_FOUND" */
        public static final String NOT_FOUND_CODE = "GET_CARD_TRANSACTIONS_NOT_FOUND";
        public static final int NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a NotFound error (404). */
        public static boolean isNotFound(SdkApiError e) {
            return NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is NotFound. */
        public boolean notFound(SdkApiError e) { return isNotFound(e); }

        /** Matches server-side cardErrors.getCardTransactions.UNAUTHORIZED_CODE = "GET_CARD_TRANSACTIONS_UNAUTHORIZED" */
        public static final String UNAUTHORIZED_CODE = "GET_CARD_TRANSACTIONS_UNAUTHORIZED";
        public static final int UNAUTHORIZED_STATUS = 401;

        /** Returns true if the SdkApiError is a Unauthorized error (401). */
        public static boolean isUnauthorized(SdkApiError e) {
            return UNAUTHORIZED_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is Unauthorized. */
        public boolean unauthorized(SdkApiError e) { return isUnauthorized(e); }

    }

}
