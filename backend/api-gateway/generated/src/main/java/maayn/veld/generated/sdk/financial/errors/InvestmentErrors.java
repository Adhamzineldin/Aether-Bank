// Code generated by Veld — DO NOT EDIT.
package maayn.veld.generated.sdk.financial.errors;

/**
 * Error constants and matchers for the Investment module.
 * Mirrors the server-side {@code InvestmentErrors} pattern.
 * <pre>{@code
 * } catch (SdkApiError e) {
 *     if (InvestmentErrors.openInvestmentAccount.isCustomerNotFound(e)) { ... }
 * }
 * }</pre>
 */
public final class InvestmentErrors {
    private InvestmentErrors() {}

    /** Error constants and matchers for the openInvestmentAccount action. */
    public static final OpenInvestmentAccountErrors openInvestmentAccount = new OpenInvestmentAccountErrors();

    /** Per-error constants and matchers for openInvestmentAccount.
     * Constants match server-side {@code InvestmentErrors.openInvestmentAccount.*_CODE} exactly. */
    public static final class OpenInvestmentAccountErrors {
        private OpenInvestmentAccountErrors() {}

        /** Matches server-side investmentErrors.openInvestmentAccount.CUSTOMER_NOT_FOUND_CODE = "OPEN_INVESTMENT_ACCOUNT_CUSTOMER_NOT_FOUND" */
        public static final String CUSTOMER_NOT_FOUND_CODE = "OPEN_INVESTMENT_ACCOUNT_CUSTOMER_NOT_FOUND";
        public static final int CUSTOMER_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a CustomerNotFound error (404). */
        public static boolean isCustomerNotFound(SdkApiError e) {
            return CUSTOMER_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is CustomerNotFound. */
        public boolean customerNotFound(SdkApiError e) { return isCustomerNotFound(e); }

        /** Matches server-side investmentErrors.openInvestmentAccount.ACCOUNT_NOT_FOUND_CODE = "OPEN_INVESTMENT_ACCOUNT_ACCOUNT_NOT_FOUND" */
        public static final String ACCOUNT_NOT_FOUND_CODE = "OPEN_INVESTMENT_ACCOUNT_ACCOUNT_NOT_FOUND";
        public static final int ACCOUNT_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a AccountNotFound error (404). */
        public static boolean isAccountNotFound(SdkApiError e) {
            return ACCOUNT_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is AccountNotFound. */
        public boolean accountNotFound(SdkApiError e) { return isAccountNotFound(e); }

        /** Matches server-side investmentErrors.openInvestmentAccount.INVALID_CURRENCY_CODE = "OPEN_INVESTMENT_ACCOUNT_INVALID_CURRENCY" */
        public static final String INVALID_CURRENCY_CODE = "OPEN_INVESTMENT_ACCOUNT_INVALID_CURRENCY";
        public static final int INVALID_CURRENCY_STATUS = 400;

        /** Returns true if the SdkApiError is a InvalidCurrency error (400). */
        public static boolean isInvalidCurrency(SdkApiError e) {
            return INVALID_CURRENCY_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InvalidCurrency. */
        public boolean invalidCurrency(SdkApiError e) { return isInvalidCurrency(e); }

    }

    /** Error constants and matchers for the getInvestmentAccount action. */
    public static final GetInvestmentAccountErrors getInvestmentAccount = new GetInvestmentAccountErrors();

    /** Per-error constants and matchers for getInvestmentAccount.
     * Constants match server-side {@code InvestmentErrors.getInvestmentAccount.*_CODE} exactly. */
    public static final class GetInvestmentAccountErrors {
        private GetInvestmentAccountErrors() {}

        /** Matches server-side investmentErrors.getInvestmentAccount.ACCOUNT_NOT_FOUND_CODE = "GET_INVESTMENT_ACCOUNT_ACCOUNT_NOT_FOUND" */
        public static final String ACCOUNT_NOT_FOUND_CODE = "GET_INVESTMENT_ACCOUNT_ACCOUNT_NOT_FOUND";
        public static final int ACCOUNT_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a AccountNotFound error (404). */
        public static boolean isAccountNotFound(SdkApiError e) {
            return ACCOUNT_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is AccountNotFound. */
        public boolean accountNotFound(SdkApiError e) { return isAccountNotFound(e); }

    }

    /** Error constants and matchers for the buyAsset action. */
    public static final BuyAssetErrors buyAsset = new BuyAssetErrors();

    /** Per-error constants and matchers for buyAsset.
     * Constants match server-side {@code InvestmentErrors.buyAsset.*_CODE} exactly. */
    public static final class BuyAssetErrors {
        private BuyAssetErrors() {}

        /** Matches server-side investmentErrors.buyAsset.ACCOUNT_NOT_FOUND_CODE = "BUY_ASSET_ACCOUNT_NOT_FOUND" */
        public static final String ACCOUNT_NOT_FOUND_CODE = "BUY_ASSET_ACCOUNT_NOT_FOUND";
        public static final int ACCOUNT_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a AccountNotFound error (404). */
        public static boolean isAccountNotFound(SdkApiError e) {
            return ACCOUNT_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is AccountNotFound. */
        public boolean accountNotFound(SdkApiError e) { return isAccountNotFound(e); }

        /** Matches server-side investmentErrors.buyAsset.ASSET_NOT_FOUND_CODE = "BUY_ASSET_ASSET_NOT_FOUND" */
        public static final String ASSET_NOT_FOUND_CODE = "BUY_ASSET_ASSET_NOT_FOUND";
        public static final int ASSET_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a AssetNotFound error (404). */
        public static boolean isAssetNotFound(SdkApiError e) {
            return ASSET_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is AssetNotFound. */
        public boolean assetNotFound(SdkApiError e) { return isAssetNotFound(e); }

        /** Matches server-side investmentErrors.buyAsset.INSUFFICIENT_FUNDS_CODE = "BUY_ASSET_INSUFFICIENT_FUNDS" */
        public static final String INSUFFICIENT_FUNDS_CODE = "BUY_ASSET_INSUFFICIENT_FUNDS";
        public static final int INSUFFICIENT_FUNDS_STATUS = 422;

        /** Returns true if the SdkApiError is a InsufficientFunds error (422). */
        public static boolean isInsufficientFunds(SdkApiError e) {
            return INSUFFICIENT_FUNDS_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InsufficientFunds. */
        public boolean insufficientFunds(SdkApiError e) { return isInsufficientFunds(e); }

        /** Matches server-side investmentErrors.buyAsset.INVALID_QUANTITY_CODE = "BUY_ASSET_INVALID_QUANTITY" */
        public static final String INVALID_QUANTITY_CODE = "BUY_ASSET_INVALID_QUANTITY";
        public static final int INVALID_QUANTITY_STATUS = 400;

        /** Returns true if the SdkApiError is a InvalidQuantity error (400). */
        public static boolean isInvalidQuantity(SdkApiError e) {
            return INVALID_QUANTITY_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InvalidQuantity. */
        public boolean invalidQuantity(SdkApiError e) { return isInvalidQuantity(e); }

        /** Matches server-side investmentErrors.buyAsset.MARKET_CLOSED_CODE = "BUY_ASSET_MARKET_CLOSED" */
        public static final String MARKET_CLOSED_CODE = "BUY_ASSET_MARKET_CLOSED";
        public static final int MARKET_CLOSED_STATUS = 400;

        /** Returns true if the SdkApiError is a MarketClosed error (400). */
        public static boolean isMarketClosed(SdkApiError e) {
            return MARKET_CLOSED_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is MarketClosed. */
        public boolean marketClosed(SdkApiError e) { return isMarketClosed(e); }

    }

    /** Error constants and matchers for the sellAsset action. */
    public static final SellAssetErrors sellAsset = new SellAssetErrors();

    /** Per-error constants and matchers for sellAsset.
     * Constants match server-side {@code InvestmentErrors.sellAsset.*_CODE} exactly. */
    public static final class SellAssetErrors {
        private SellAssetErrors() {}

        /** Matches server-side investmentErrors.sellAsset.ACCOUNT_NOT_FOUND_CODE = "SELL_ASSET_ACCOUNT_NOT_FOUND" */
        public static final String ACCOUNT_NOT_FOUND_CODE = "SELL_ASSET_ACCOUNT_NOT_FOUND";
        public static final int ACCOUNT_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a AccountNotFound error (404). */
        public static boolean isAccountNotFound(SdkApiError e) {
            return ACCOUNT_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is AccountNotFound. */
        public boolean accountNotFound(SdkApiError e) { return isAccountNotFound(e); }

        /** Matches server-side investmentErrors.sellAsset.ASSET_NOT_FOUND_CODE = "SELL_ASSET_ASSET_NOT_FOUND" */
        public static final String ASSET_NOT_FOUND_CODE = "SELL_ASSET_ASSET_NOT_FOUND";
        public static final int ASSET_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a AssetNotFound error (404). */
        public static boolean isAssetNotFound(SdkApiError e) {
            return ASSET_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is AssetNotFound. */
        public boolean assetNotFound(SdkApiError e) { return isAssetNotFound(e); }

        /** Matches server-side investmentErrors.sellAsset.INSUFFICIENT_HOLDING_CODE = "SELL_ASSET_INSUFFICIENT_HOLDING" */
        public static final String INSUFFICIENT_HOLDING_CODE = "SELL_ASSET_INSUFFICIENT_HOLDING";
        public static final int INSUFFICIENT_HOLDING_STATUS = 422;

        /** Returns true if the SdkApiError is a InsufficientHolding error (422). */
        public static boolean isInsufficientHolding(SdkApiError e) {
            return INSUFFICIENT_HOLDING_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InsufficientHolding. */
        public boolean insufficientHolding(SdkApiError e) { return isInsufficientHolding(e); }

        /** Matches server-side investmentErrors.sellAsset.INVALID_QUANTITY_CODE = "SELL_ASSET_INVALID_QUANTITY" */
        public static final String INVALID_QUANTITY_CODE = "SELL_ASSET_INVALID_QUANTITY";
        public static final int INVALID_QUANTITY_STATUS = 400;

        /** Returns true if the SdkApiError is a InvalidQuantity error (400). */
        public static boolean isInvalidQuantity(SdkApiError e) {
            return INVALID_QUANTITY_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InvalidQuantity. */
        public boolean invalidQuantity(SdkApiError e) { return isInvalidQuantity(e); }

        /** Matches server-side investmentErrors.sellAsset.MARKET_CLOSED_CODE = "SELL_ASSET_MARKET_CLOSED" */
        public static final String MARKET_CLOSED_CODE = "SELL_ASSET_MARKET_CLOSED";
        public static final int MARKET_CLOSED_STATUS = 400;

        /** Returns true if the SdkApiError is a MarketClosed error (400). */
        public static boolean isMarketClosed(SdkApiError e) {
            return MARKET_CLOSED_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is MarketClosed. */
        public boolean marketClosed(SdkApiError e) { return isMarketClosed(e); }

    }

    /** Error constants and matchers for the getPortfolio action. */
    public static final GetPortfolioErrors getPortfolio = new GetPortfolioErrors();

    /** Per-error constants and matchers for getPortfolio.
     * Constants match server-side {@code InvestmentErrors.getPortfolio.*_CODE} exactly. */
    public static final class GetPortfolioErrors {
        private GetPortfolioErrors() {}

        /** Matches server-side investmentErrors.getPortfolio.ACCOUNT_NOT_FOUND_CODE = "GET_PORTFOLIO_ACCOUNT_NOT_FOUND" */
        public static final String ACCOUNT_NOT_FOUND_CODE = "GET_PORTFOLIO_ACCOUNT_NOT_FOUND";
        public static final int ACCOUNT_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a AccountNotFound error (404). */
        public static boolean isAccountNotFound(SdkApiError e) {
            return ACCOUNT_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is AccountNotFound. */
        public boolean accountNotFound(SdkApiError e) { return isAccountNotFound(e); }

    }

    /** Error constants and matchers for the getPerformance action. */
    public static final GetPerformanceErrors getPerformance = new GetPerformanceErrors();

    /** Per-error constants and matchers for getPerformance.
     * Constants match server-side {@code InvestmentErrors.getPerformance.*_CODE} exactly. */
    public static final class GetPerformanceErrors {
        private GetPerformanceErrors() {}

        /** Matches server-side investmentErrors.getPerformance.ACCOUNT_NOT_FOUND_CODE = "GET_PERFORMANCE_ACCOUNT_NOT_FOUND" */
        public static final String ACCOUNT_NOT_FOUND_CODE = "GET_PERFORMANCE_ACCOUNT_NOT_FOUND";
        public static final int ACCOUNT_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a AccountNotFound error (404). */
        public static boolean isAccountNotFound(SdkApiError e) {
            return ACCOUNT_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is AccountNotFound. */
        public boolean accountNotFound(SdkApiError e) { return isAccountNotFound(e); }

    }

    /** Error constants and matchers for the getAsset action. */
    public static final GetAssetErrors getAsset = new GetAssetErrors();

    /** Per-error constants and matchers for getAsset.
     * Constants match server-side {@code InvestmentErrors.getAsset.*_CODE} exactly. */
    public static final class GetAssetErrors {
        private GetAssetErrors() {}

        /** Matches server-side investmentErrors.getAsset.ASSET_NOT_FOUND_CODE = "GET_ASSET_ASSET_NOT_FOUND" */
        public static final String ASSET_NOT_FOUND_CODE = "GET_ASSET_ASSET_NOT_FOUND";
        public static final int ASSET_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a AssetNotFound error (404). */
        public static boolean isAssetNotFound(SdkApiError e) {
            return ASSET_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is AssetNotFound. */
        public boolean assetNotFound(SdkApiError e) { return isAssetNotFound(e); }

    }

}
