// Code generated by Veld — DO NOT EDIT.
package maayn.veld.generated.sdk.card;

import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.util.Map;
import java.util.HashMap;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.SerializationFeature;
import com.fasterxml.jackson.datatype.jsr310.JavaTimeModule;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;
import maayn.veld.generated.sdk.card.errors.SdkApiError;
import maayn.veld.generated.sdk.card.models.card.*;

@Component
public class CardClient {
    private final String base;
    private final HttpClient http;
    private final Map<String, String> headers;
    private final ObjectMapper mapper = new ObjectMapper()
        .registerModule(new JavaTimeModule())
        .disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);

    /** Actions for the Card module. Usage: client.card.actionName(...) */
    public final CardActions card = new CardActions();

    /**
     * Spring-managed constructor.
     * Configure via {@code veld.sdk.card.base-url} in application.properties,
     * or set the {@code VELD_CARD_URL} environment variable.
     */
    @Autowired
    public CardClient(@Value("${veld.sdk.card.base-url:${VELD_CARD_URL:http://card-service:3002}}") String baseUrl) {
        this.base = baseUrl;
        this.http = HttpClient.newHttpClient();
        this.headers = new HashMap<>();
    }

    /** Manual constructor for non-Spring usage or testing. */
    public CardClient(String baseUrl, Map<String, String> headers) {
        this.base = baseUrl != null ? baseUrl : System.getenv("VELD_CARD_URL") != null ? System.getenv("VELD_CARD_URL") : "http://card-service:3002";
        this.http = HttpClient.newHttpClient();
        this.headers = headers != null ? headers : new HashMap<>();
    }

    String request(String method, String path, String body) throws Exception {
        HttpRequest.Builder builder = HttpRequest.newBuilder(URI.create(this.base + path))
            .header("Content-Type", "application/json");
        this.headers.forEach(builder::header);
        if (body != null) { builder.method(method, HttpRequest.BodyPublishers.ofString(body)); }
        else { builder.method(method, HttpRequest.BodyPublishers.noBody()); }
        HttpResponse<String> res = http.send(builder.build(), HttpResponse.BodyHandlers.ofString());
        if (res.statusCode() >= 400) {
            String errBody = res.body();
            String code = extractJsonString(errBody, "code");
            String msg  = extractJsonString(errBody, "error");
            if (code == null) code = "UNKNOWN_ERROR";
            if (msg  == null) msg  = errBody;
            throw new SdkApiError(code, res.statusCode(), msg);
        }
        return res.body();
    }

    ObjectMapper mapper() { return mapper; }

    static String extractJsonString(String json, String key) {
        if (json == null) return null;
        String search = "\"" + key + "\"";
        int idx = json.indexOf(search);
        if (idx < 0) return null;
        int colon = json.indexOf(':', idx + search.length());
        if (colon < 0) return null;
        int start = json.indexOf('"', colon + 1);
        if (start < 0) return null;
        int end = json.indexOf('"', start + 1);
        if (end < 0) return null;
        return json.substring(start + 1, end);
    }

    /** Actions for the Card module. */
    public final class CardActions {
        private CardActions() {}

        /** GET /api/card/:cardId */
        public CardDetailsResponse getCardDetails(String cardId) throws Exception {
            String raw = request("GET", "/api/card/" + cardId, null);
            return mapper().readValue(raw, CardDetailsResponse.class);
        }

        /** GET /api/card/:cardId/pan */
        public PanRevealResponse revealPan(String cardId) throws Exception {
            String raw = request("GET", "/api/card/" + cardId + "/pan", null);
            return mapper().readValue(raw, PanRevealResponse.class);
        }

        /** POST /api/card/payments */
        public CardTransactionResponse processMerchantPayment(MerchantPaymentRequest input) throws Exception {
            String body = mapper().writeValueAsString(input);
            String raw = request("POST", "/api/card/payments", body);
            return mapper().readValue(raw, CardTransactionResponse.class);
        }

        /** POST /api/card/refunds */
        public CardTransactionResponse refundTransaction(RefundCardTransactionRequest input) throws Exception {
            String body = mapper().writeValueAsString(input);
            String raw = request("POST", "/api/card/refunds", body);
            return mapper().readValue(raw, CardTransactionResponse.class);
        }

        /** POST /api/card/voids */
        public CardTransactionResponse voidTransaction(VoidCardTransactionRequest input) throws Exception {
            String body = mapper().writeValueAsString(input);
            String raw = request("POST", "/api/card/voids", body);
            return mapper().readValue(raw, CardTransactionResponse.class);
        }

        /** GET /api/card/:cardId/transactions */
        public PaginatedCardTransactionResponse getCardTransactions(String cardId, GetCardTransactionsRequest input) throws Exception {
            String body = mapper().writeValueAsString(input);
            String raw = request("GET", "/api/card/" + cardId + "/transactions", body);
            return mapper().readValue(raw, PaginatedCardTransactionResponse.class);
        }
    }

}
