// Code generated by Veld — DO NOT EDIT.
package maayn.veld.generated.sdk.notification.errors;

/**
 * Error constants and matchers for the Notification module.
 * Mirrors the server-side {@code NotificationErrors} pattern.
 * <pre>{@code
 * } catch (SdkApiError e) {
 *     if (NotificationErrors.getNotification.isNotFound(e)) { ... }
 * }
 * }</pre>
 */
public final class NotificationErrors {
    private NotificationErrors() {}

    /** Error constants and matchers for the GetNotification action. */
    public static final GetNotificationErrors getNotification = new GetNotificationErrors();

    /** Per-error constants and matchers for GetNotification.
     * Constants match server-side {@code NotificationErrors.getNotification.*_CODE} exactly. */
    public static final class GetNotificationErrors {
        private GetNotificationErrors() {}

        /** Matches server-side notificationErrors.getNotification.NOT_FOUND_CODE = "GET_NOTIFICATION_NOT_FOUND" */
        public static final String NOT_FOUND_CODE = "GET_NOTIFICATION_NOT_FOUND";
        public static final int NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a NotFound error (404). */
        public static boolean isNotFound(SdkApiError e) {
            return NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is NotFound. */
        public boolean notFound(SdkApiError e) { return isNotFound(e); }

        /** Matches server-side notificationErrors.getNotification.FORBIDDEN_CODE = "GET_NOTIFICATION_FORBIDDEN" */
        public static final String FORBIDDEN_CODE = "GET_NOTIFICATION_FORBIDDEN";
        public static final int FORBIDDEN_STATUS = 403;

        /** Returns true if the SdkApiError is a Forbidden error (403). */
        public static boolean isForbidden(SdkApiError e) {
            return FORBIDDEN_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is Forbidden. */
        public boolean forbidden(SdkApiError e) { return isForbidden(e); }

    }

    /** Error constants and matchers for the SendNotification action. */
    public static final SendNotificationErrors sendNotification = new SendNotificationErrors();

    /** Per-error constants and matchers for SendNotification.
     * Constants match server-side {@code NotificationErrors.sendNotification.*_CODE} exactly. */
    public static final class SendNotificationErrors {
        private SendNotificationErrors() {}

        /** Matches server-side notificationErrors.sendNotification.NOT_FOUND_CODE = "SEND_NOTIFICATION_NOT_FOUND" */
        public static final String NOT_FOUND_CODE = "SEND_NOTIFICATION_NOT_FOUND";
        public static final int NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a NotFound error (404). */
        public static boolean isNotFound(SdkApiError e) {
            return NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is NotFound. */
        public boolean notFound(SdkApiError e) { return isNotFound(e); }

        /** Matches server-side notificationErrors.sendNotification.BAD_REQUEST_CODE = "SEND_NOTIFICATION_BAD_REQUEST" */
        public static final String BAD_REQUEST_CODE = "SEND_NOTIFICATION_BAD_REQUEST";
        public static final int BAD_REQUEST_STATUS = 400;

        /** Returns true if the SdkApiError is a BadRequest error (400). */
        public static boolean isBadRequest(SdkApiError e) {
            return BAD_REQUEST_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is BadRequest. */
        public boolean badRequest(SdkApiError e) { return isBadRequest(e); }

    }

    /** Error constants and matchers for the RetryNotification action. */
    public static final RetryNotificationErrors retryNotification = new RetryNotificationErrors();

    /** Per-error constants and matchers for RetryNotification.
     * Constants match server-side {@code NotificationErrors.retryNotification.*_CODE} exactly. */
    public static final class RetryNotificationErrors {
        private RetryNotificationErrors() {}

        /** Matches server-side notificationErrors.retryNotification.NOT_FOUND_CODE = "RETRY_NOTIFICATION_NOT_FOUND" */
        public static final String NOT_FOUND_CODE = "RETRY_NOTIFICATION_NOT_FOUND";
        public static final int NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a NotFound error (404). */
        public static boolean isNotFound(SdkApiError e) {
            return NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is NotFound. */
        public boolean notFound(SdkApiError e) { return isNotFound(e); }

        /** Matches server-side notificationErrors.retryNotification.BAD_REQUEST_CODE = "RETRY_NOTIFICATION_BAD_REQUEST" */
        public static final String BAD_REQUEST_CODE = "RETRY_NOTIFICATION_BAD_REQUEST";
        public static final int BAD_REQUEST_STATUS = 400;

        /** Returns true if the SdkApiError is a BadRequest error (400). */
        public static boolean isBadRequest(SdkApiError e) {
            return BAD_REQUEST_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is BadRequest. */
        public boolean badRequest(SdkApiError e) { return isBadRequest(e); }

    }

    /** Error constants and matchers for the GetTemplate action. */
    public static final GetTemplateErrors getTemplate = new GetTemplateErrors();

    /** Per-error constants and matchers for GetTemplate.
     * Constants match server-side {@code NotificationErrors.getTemplate.*_CODE} exactly. */
    public static final class GetTemplateErrors {
        private GetTemplateErrors() {}

        /** Matches server-side notificationErrors.getTemplate.NOT_FOUND_CODE = "GET_TEMPLATE_NOT_FOUND" */
        public static final String NOT_FOUND_CODE = "GET_TEMPLATE_NOT_FOUND";
        public static final int NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a NotFound error (404). */
        public static boolean isNotFound(SdkApiError e) {
            return NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is NotFound. */
        public boolean notFound(SdkApiError e) { return isNotFound(e); }

    }

    /** Error constants and matchers for the CreateTemplate action. */
    public static final CreateTemplateErrors createTemplate = new CreateTemplateErrors();

    /** Per-error constants and matchers for CreateTemplate.
     * Constants match server-side {@code NotificationErrors.createTemplate.*_CODE} exactly. */
    public static final class CreateTemplateErrors {
        private CreateTemplateErrors() {}

        /** Matches server-side notificationErrors.createTemplate.BAD_REQUEST_CODE = "CREATE_TEMPLATE_BAD_REQUEST" */
        public static final String BAD_REQUEST_CODE = "CREATE_TEMPLATE_BAD_REQUEST";
        public static final int BAD_REQUEST_STATUS = 400;

        /** Returns true if the SdkApiError is a BadRequest error (400). */
        public static boolean isBadRequest(SdkApiError e) {
            return BAD_REQUEST_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is BadRequest. */
        public boolean badRequest(SdkApiError e) { return isBadRequest(e); }

    }

    /** Error constants and matchers for the UpdateTemplate action. */
    public static final UpdateTemplateErrors updateTemplate = new UpdateTemplateErrors();

    /** Per-error constants and matchers for UpdateTemplate.
     * Constants match server-side {@code NotificationErrors.updateTemplate.*_CODE} exactly. */
    public static final class UpdateTemplateErrors {
        private UpdateTemplateErrors() {}

        /** Matches server-side notificationErrors.updateTemplate.NOT_FOUND_CODE = "UPDATE_TEMPLATE_NOT_FOUND" */
        public static final String NOT_FOUND_CODE = "UPDATE_TEMPLATE_NOT_FOUND";
        public static final int NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a NotFound error (404). */
        public static boolean isNotFound(SdkApiError e) {
            return NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is NotFound. */
        public boolean notFound(SdkApiError e) { return isNotFound(e); }

        /** Matches server-side notificationErrors.updateTemplate.BAD_REQUEST_CODE = "UPDATE_TEMPLATE_BAD_REQUEST" */
        public static final String BAD_REQUEST_CODE = "UPDATE_TEMPLATE_BAD_REQUEST";
        public static final int BAD_REQUEST_STATUS = 400;

        /** Returns true if the SdkApiError is a BadRequest error (400). */
        public static boolean isBadRequest(SdkApiError e) {
            return BAD_REQUEST_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is BadRequest. */
        public boolean badRequest(SdkApiError e) { return isBadRequest(e); }

    }

    /** Error constants and matchers for the DeactivateTemplate action. */
    public static final DeactivateTemplateErrors deactivateTemplate = new DeactivateTemplateErrors();

    /** Per-error constants and matchers for DeactivateTemplate.
     * Constants match server-side {@code NotificationErrors.deactivateTemplate.*_CODE} exactly. */
    public static final class DeactivateTemplateErrors {
        private DeactivateTemplateErrors() {}

        /** Matches server-side notificationErrors.deactivateTemplate.NOT_FOUND_CODE = "DEACTIVATE_TEMPLATE_NOT_FOUND" */
        public static final String NOT_FOUND_CODE = "DEACTIVATE_TEMPLATE_NOT_FOUND";
        public static final int NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a NotFound error (404). */
        public static boolean isNotFound(SdkApiError e) {
            return NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is NotFound. */
        public boolean notFound(SdkApiError e) { return isNotFound(e); }

    }

}
