// Code generated by Veld — DO NOT EDIT.
package maayn.veld.generated.sdk.iam.errors;

/**
 * Error constants and matchers for the Authentication module.
 * Mirrors the server-side {@code AuthenticationErrors} pattern.
 * <pre>{@code
 * } catch (SdkApiError e) {
 *     if (AuthenticationErrors.login.isUserNotFound(e)) { ... }
 * }
 * }</pre>
 */
public final class AuthenticationErrors {
    private AuthenticationErrors() {}

    /** Error constants and matchers for the login action. */
    public static final LoginErrors login = new LoginErrors();

    /** Per-error constants and matchers for login.
     * Constants match server-side {@code AuthenticationErrors.login.*_CODE} exactly. */
    public static final class LoginErrors {
        private LoginErrors() {}

        /** Matches server-side authenticationErrors.login.USER_NOT_FOUND_CODE = "LOGIN_USER_NOT_FOUND" */
        public static final String USER_NOT_FOUND_CODE = "LOGIN_USER_NOT_FOUND";
        public static final int USER_NOT_FOUND_STATUS = 404;

        /** Returns true if the SdkApiError is a UserNotFound error (404). */
        public static boolean isUserNotFound(SdkApiError e) {
            return USER_NOT_FOUND_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is UserNotFound. */
        public boolean userNotFound(SdkApiError e) { return isUserNotFound(e); }

        /** Matches server-side authenticationErrors.login.INVALID_CREDENTIALS_CODE = "LOGIN_INVALID_CREDENTIALS" */
        public static final String INVALID_CREDENTIALS_CODE = "LOGIN_INVALID_CREDENTIALS";
        public static final int INVALID_CREDENTIALS_STATUS = 401;

        /** Returns true if the SdkApiError is a InvalidCredentials error (401). */
        public static boolean isInvalidCredentials(SdkApiError e) {
            return INVALID_CREDENTIALS_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InvalidCredentials. */
        public boolean invalidCredentials(SdkApiError e) { return isInvalidCredentials(e); }

    }

}
