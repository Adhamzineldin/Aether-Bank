// Code generated by Veld — DO NOT EDIT.
package maayn.veld.generated.sdk.transaction.errors;

/**
 * Error constants and matchers for the Transaction module.
 * Mirrors the server-side {@code TransactionErrors} pattern.
 * <pre>{@code
 * } catch (SdkApiError e) {
 *     if (TransactionErrors.transfer.isInsufficientFunds(e)) { ... }
 * }
 * }</pre>
 */
public final class TransactionErrors {
    private TransactionErrors() {}

    /** Error constants and matchers for the transfer action. */
    public static final TransferErrors transfer = new TransferErrors();

    /** Per-error constants and matchers for transfer.
     * Constants match server-side {@code TransactionErrors.transfer.*_CODE} exactly. */
    public static final class TransferErrors {
        private TransferErrors() {}

        /** Matches server-side transactionErrors.transfer.INSUFFICIENT_FUNDS_CODE = "TRANSFER_INSUFFICIENT_FUNDS" */
        public static final String INSUFFICIENT_FUNDS_CODE = "TRANSFER_INSUFFICIENT_FUNDS";
        public static final int INSUFFICIENT_FUNDS_STATUS = 403;

        /** Returns true if the SdkApiError is a InsufficientFunds error (403). */
        public static boolean isInsufficientFunds(SdkApiError e) {
            return INSUFFICIENT_FUNDS_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InsufficientFunds. */
        public boolean insufficientFunds(SdkApiError e) { return isInsufficientFunds(e); }

        /** Matches server-side transactionErrors.transfer.INVALID_ACCOUNT_CODE = "TRANSFER_INVALID_ACCOUNT" */
        public static final String INVALID_ACCOUNT_CODE = "TRANSFER_INVALID_ACCOUNT";
        public static final int INVALID_ACCOUNT_STATUS = 404;

        /** Returns true if the SdkApiError is a InvalidAccount error (404). */
        public static boolean isInvalidAccount(SdkApiError e) {
            return INVALID_ACCOUNT_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InvalidAccount. */
        public boolean invalidAccount(SdkApiError e) { return isInvalidAccount(e); }

        /** Matches server-side transactionErrors.transfer.UNAUTHORIZED_CODE = "TRANSFER_UNAUTHORIZED" */
        public static final String UNAUTHORIZED_CODE = "TRANSFER_UNAUTHORIZED";
        public static final int UNAUTHORIZED_STATUS = 401;

        /** Returns true if the SdkApiError is a Unauthorized error (401). */
        public static boolean isUnauthorized(SdkApiError e) {
            return UNAUTHORIZED_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is Unauthorized. */
        public boolean unauthorized(SdkApiError e) { return isUnauthorized(e); }

        /** Matches server-side transactionErrors.transfer.INVALID_AMOUNT_CODE = "TRANSFER_INVALID_AMOUNT" */
        public static final String INVALID_AMOUNT_CODE = "TRANSFER_INVALID_AMOUNT";
        public static final int INVALID_AMOUNT_STATUS = 400;

        /** Returns true if the SdkApiError is a InvalidAmount error (400). */
        public static boolean isInvalidAmount(SdkApiError e) {
            return INVALID_AMOUNT_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is InvalidAmount. */
        public boolean invalidAmount(SdkApiError e) { return isInvalidAmount(e); }

        /** Matches server-side transactionErrors.transfer.INVALID_TARGET_CODE = "TRANSFER_INVALID_TARGET" */
        public static final String INVALID_TARGET_CODE = "TRANSFER_INVALID_TARGET";
        public static final int INVALID_TARGET_STATUS = 400;

        /** Returns true if the SdkApiError is a invalidTarget error (400). */
        public static boolean isInvalidTarget(SdkApiError e) {
            return INVALID_TARGET_CODE.equals(e.getCode());
        }

        /** Returns true if this instance's error is invalidTarget. */
        public boolean invalidTarget(SdkApiError e) { return isInvalidTarget(e); }

    }

}
