import "../models/shared.model.veld"

module shared {
  description: "IAM shared endpoints"
  prefix: /api/auth

  action getPublicKey {
    method: GET
    path: /public-key
    output: PublicKeyResponse
  }
}
