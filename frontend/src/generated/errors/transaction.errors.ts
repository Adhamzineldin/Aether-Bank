// AUTO-GENERATED BY VELD — DO NOT EDIT

import { ApiError } from './_base';
export { ApiError } from './_base';

/** Error codes for transfer */
export type transferErrorCode = 'TRANSFER_INSUFFICIENT_FUNDS' | 'TRANSFER_INVALID_ACCOUNT' | 'TRANSFER_UNAUTHORIZED' | 'TRANSFER_INVALID_AMOUNT' | 'TRANSFER_INVALID_TARGET';

/** Typed error for transfer */
export type transferError = ApiError<transferErrorCode>;

/** Error factories for transfer — each factory also exposes .code and .status for frontend type guards. */
export const transferErrors = {
  insufficientFunds: Object.assign(
    (message?: string): transferError => new ApiError('TRANSFER_INSUFFICIENT_FUNDS', 403, message ?? 'InsufficientFunds'),
    { code: 'TRANSFER_INSUFFICIENT_FUNDS' as const, status: 403 as const },
  ),
  invalidAccount: Object.assign(
    (message?: string): transferError => new ApiError('TRANSFER_INVALID_ACCOUNT', 404, message ?? 'InvalidAccount'),
    { code: 'TRANSFER_INVALID_ACCOUNT' as const, status: 404 as const },
  ),
  unauthorized: Object.assign(
    (message?: string): transferError => new ApiError('TRANSFER_UNAUTHORIZED', 401, message ?? 'Unauthorized'),
    { code: 'TRANSFER_UNAUTHORIZED' as const, status: 401 as const },
  ),
  invalidAmount: Object.assign(
    (message?: string): transferError => new ApiError('TRANSFER_INVALID_AMOUNT', 400, message ?? 'InvalidAmount'),
    { code: 'TRANSFER_INVALID_AMOUNT' as const, status: 400 as const },
  ),
  invalidTarget: Object.assign(
    (message?: string): transferError => new ApiError('TRANSFER_INVALID_TARGET', 400, message ?? 'invalidTarget'),
    { code: 'TRANSFER_INVALID_TARGET' as const, status: 400 as const },
  ),
} as const;

export const transactionErrors = {
  transfer: transferErrors,
} as const;
