// AUTO-GENERATED BY VELD — DO NOT EDIT

import { ApiError } from './_base';
export { ApiError } from './_base';

/** Error codes for login */
export type loginErrorCode = 'LOGIN_USER_NOT_FOUND' | 'LOGIN_INVALID_CREDENTIALS';

/** Typed error for login */
export type loginError = ApiError<loginErrorCode>;

/** Error factories for login — each factory also exposes .code and .status for frontend type guards. */
export const loginErrors = {
  userNotFound: Object.assign(
    (message?: string): loginError => new ApiError('LOGIN_USER_NOT_FOUND', 404, message ?? 'UserNotFound'),
    { code: 'LOGIN_USER_NOT_FOUND' as const, status: 404 as const },
  ),
  invalidCredentials: Object.assign(
    (message?: string): loginError => new ApiError('LOGIN_INVALID_CREDENTIALS', 401, message ?? 'InvalidCredentials'),
    { code: 'LOGIN_INVALID_CREDENTIALS' as const, status: 401 as const },
  ),
} as const;

export const authenticationErrors = {
  login: loginErrors,
} as const;
