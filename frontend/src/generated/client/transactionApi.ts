// AUTO-GENERATED BY VELD — DO NOT EDIT
import { VeldApiError, resolveBase } from './_internal';
import type { VeldClientConfig } from './_internal';
import type { TransferRequest, TransactionResponse, getAccountTransactionsRequest, PaginatedTransactionResponse, AssetHolding, AssetType, InvestmentTransaction, TransactionStatus, TransactionType } from '../types';

class TransactionClient {
  private readonly base: string;
  private readonly hdrs: Record<string, string>;

  constructor(config?: VeldClientConfig | string) {
    this.base = resolveBase(config) || "http://localhost:2501";
    this.hdrs = typeof config === 'object' && config !== null
      ? (config as VeldClientConfig).headers ?? {}
      : {};
  }

  private async request<T>(method: string, path: string, body?: unknown, extraHeaders?: Record<string, string>): Promise<T> {
    const res = await fetch(this.base + path, {
      method,
      headers: { 'Content-Type': 'application/json', ...this.hdrs, ...extraHeaders },
      body: body !== undefined ? JSON.stringify(body) : undefined,
    });
    if (!res.ok) throw new VeldApiError(res.status, await res.text());
    if (res.status === 204) return undefined as T;
    return res.json() as Promise<T>;
  }

  /** POST /api/transaction_service/transactions/transfer */
  transfer(input: TransferRequest): Promise<TransactionResponse> {
    return this.request('POST', '/api/transaction_service/transactions/transfer', input);
  }

  /** GET /api/transaction_service/transactions/account/:acountId */
  getAccountTransactions(acountId: string, input: getAccountTransactionsRequest): Promise<PaginatedTransactionResponse> {
    return this.request('GET', `/api/transaction_service/transactions/account/${acountId}`, undefined);
  }

  readonly errors = {
    transfer: {
      insufficientFunds: 'TRANSFER_INSUFFICIENT_FUNDS',
      invalidAccount: 'TRANSFER_INVALID_ACCOUNT',
      unauthorized: 'TRANSFER_UNAUTHORIZED',
      invalidAmount: 'TRANSFER_INVALID_AMOUNT',
      invalidTarget: 'TRANSFER_INVALID_TARGET',
    },
  } as const;
}

export { TransactionClient };
export const transactionApi = new TransactionClient();
