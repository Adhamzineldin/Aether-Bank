// AUTO-GENERATED BY VELD — DO NOT EDIT
import { VeldApiError, resolveBase } from './_internal';
import type { VeldClientConfig } from './_internal';
import type { CreateWorkflowInput, WorkflowInstance, UpdateWorkflowInput, ApprovalTask, TaskActionInput, WorkflowStatus, StepRole, TaskStatus, DecisionStatus, TaskDecision } from '../types';

class WorkflowClient {
  private readonly base: string;
  private readonly hdrs: Record<string, string>;

  constructor(config?: VeldClientConfig | string) {
    this.base = resolveBase(config) || "http://notification-service:3005";
    this.hdrs = typeof config === 'object' && config !== null
      ? (config as VeldClientConfig).headers ?? {}
      : {};
  }

  private async request<T>(method: string, path: string, body?: unknown, extraHeaders?: Record<string, string>): Promise<T> {
    const res = await fetch(this.base + path, {
      method,
      headers: { 'Content-Type': 'application/json', ...this.hdrs, ...extraHeaders },
      body: body !== undefined ? JSON.stringify(body) : undefined,
    });
    if (!res.ok) throw new VeldApiError(res.status, await res.text());
    if (res.status === 204) return undefined as T;
    return res.json() as Promise<T>;
  }

  /** POST /api/workflow/ */
  createWorkflow(input: CreateWorkflowInput): Promise<WorkflowInstance> {
    return this.request('POST', '/api/workflow/', input);
  }

  /** PUT /api/workflow/:id */
  updateWorkflow(id: string, input: UpdateWorkflowInput): Promise<WorkflowInstance> {
    return this.request('PUT', `/api/workflow/${id}`, input);
  }

  /** GET /api/workflow/:id */
  getWorkflow(id: string): Promise<WorkflowInstance> {
    return this.request('GET', `/api/workflow/${id}`, undefined);
  }

  /** GET /api/workflow/ */
  getWorkflows(): Promise<WorkflowInstance[]> {
    return this.request('GET', '/api/workflow/', undefined);
  }

  /** GET /api/workflow/tasks/:id */
  getTasks(id: string): Promise<ApprovalTask[]> {
    return this.request('GET', `/api/workflow/tasks/${id}`, undefined);
  }

  /** POST /api/workflow/tasks/:taskId/decision */
  decideTask(taskId: string, input: TaskActionInput): Promise<ApprovalTask> {
    return this.request('POST', `/api/workflow/tasks/${taskId}/decision`, input);
  }
}

export { WorkflowClient };
export const workflowApi = new WorkflowClient();
