// AUTO-GENERATED BY VELD — DO NOT EDIT
import type { IInvestmentService } from '../interfaces/IInvestmentService';
import { ApiError } from '../errors/investment.errors';

export function investmentRouter(router: any, service: IInvestmentService): void {

  // Open a new investment account

  router.post('/api/financial_service/api/investment/account', async (req: any, res: any) => {
    try {
      const result = await service.openInvestmentAccount(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Get investment account details

  router.get('/api/financial_service/api/investment/account/:accountId', async (req: any, res: any) => {
    try {
      const result = await service.getInvestmentAccount(req.params.accountId);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Purchase an investment asset

  router.post('/api/financial_service/api/investment/account/:accountId/buy', async (req: any, res: any) => {
    try {
      const result = await service.buyAsset(req.params.accountId, req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Sell an investment asset

  router.post('/api/financial_service/api/investment/account/:accountId/sell', async (req: any, res: any) => {
    try {
      const result = await service.sellAsset(req.params.accountId, req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Get portfolio holdings and valuation

  router.get('/api/financial_service/api/investment/account/:accountId/portfolio', async (req: any, res: any) => {
    try {
      const result = await service.getPortfolio(req.params.accountId, req.body);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Get investment performance metrics

  router.get('/api/financial_service/api/investment/account/:accountId/performance', async (req: any, res: any) => {
    try {
      const result = await service.getPerformance(req.params.accountId, req.body);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // List available assets for trading

  router.get('/api/financial_service/api/investment/assets', async (req: any, res: any) => {
    try {
      const result = await service.listAssets();
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Get asset details and current price

  router.get('/api/financial_service/api/investment/assets/:symbol', async (req: any, res: any) => {
    try {
      const result = await service.getAsset(req.params.symbol);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });
}
