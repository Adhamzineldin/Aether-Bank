// AUTO-GENERATED BY VELD — DO NOT EDIT
import type { IMortgageService } from '../interfaces/IMortgageService';
import { ApiError } from '../errors/mortgage.errors';

export function mortgageRouter(router: any, service: IMortgageService): void {

  // Submit a new mortgage application

  router.post('/api/financial_service/mortgages/application', async (req: any, res: any) => {
    try {
      const result = await service.submitMortgageApplication(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Get mortgage details by ID

  router.get('/api/financial_service/mortgages/:mortgageId', async (req: any, res: any) => {
    try {
      const result = await service.getMortgage(req.params.mortgageId);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // List all mortgages for a customer

  router.get('/api/financial_service/mortgages/customer/:customerId', async (req: any, res: any) => {
    try {
      const result = await service.listCustomerMortgages(req.params.customerId);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Get repayment schedule for a mortgage

  router.get('/api/financial_service/mortgages/:mortgageId/schedule', async (req: any, res: any) => {
    try {
      const result = await service.getMortgageSchedule(req.params.mortgageId);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });
}
