// AUTO-GENERATED BY VELD — DO NOT EDIT
import type { ILedgerService } from '../interfaces/ILedgerService';
import { ApiError } from '../errors/ledger.errors';

export function ledgerRouter(router: any, service: ILedgerService): void {

  router.get('/api/transaction_service/ledger/:accountId/:currency/balance', async (req: any, res: any) => {
    try {
      const result = await service.getAccountBalance(req.params.accountId, req.params.currency);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });
}
