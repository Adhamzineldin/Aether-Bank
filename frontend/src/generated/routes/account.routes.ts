// AUTO-GENERATED BY VELD — DO NOT EDIT
import type { IAccountService } from '../interfaces/IAccountService';
import { ApiError } from '../errors/account.errors';

export function accountRouter(router: any, service: IAccountService): void {

  // Open a new bank account

  router.post('/api/accounts_service/account/', async (req: any, res: any) => {
    try {
      const result = await service.openAccount(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Get account details with balance

  router.get('/api/accounts_service/account/:accountId', async (req: any, res: any) => {
    try {
      const result = await service.getAccount(req.params.accountId);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // List all accounts for a customer

  router.get('/api/accounts_service/account/customer/:customerId', async (req: any, res: any) => {
    try {
      const result = await service.listCustomerAccounts(req.params.customerId);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Close an account

  router.post('/api/accounts_service/account/:accountId/close', async (req: any, res: any) => {
    try {
      const result = await service.closeAccount(req.params.accountId, req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Update account status (freeze/activate)

  router.put('/api/accounts_service/account/:accountId/status', async (req: any, res: any) => {
    try {
      const result = await service.updateAccountStatus(req.params.accountId, req.body);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Check if account exists

  router.get('/api/accounts_service/account/:accountId/exists', async (req: any, res: any) => {
    try {
      const result = await service.doesAccountExist(req.params.accountId);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });
}
