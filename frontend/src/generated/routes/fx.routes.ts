// AUTO-GENERATED BY VELD — DO NOT EDIT
import type { IFXService } from '../interfaces/IFXService';
import { ApiError } from '../errors/fx.errors';

export function fxRouter(router: any, service: IFXService): void {

  router.get('/api/financial_service/fx/exchange-rate', async (req: any, res: any) => {
    try {
      const result = await service.getExchangeRate(req.body);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });
}
