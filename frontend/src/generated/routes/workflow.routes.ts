// AUTO-GENERATED BY VELD — DO NOT EDIT
import type { IWorkflowService } from '../interfaces/IWorkflowService';

export function workflowRouter(router: any, service: IWorkflowService): void {

  router.post('/api/workflow/', async (req: any, res: any) => {
    try {
      const result = await service.createWorkflow(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.put('/api/workflow/:id', async (req: any, res: any) => {
    try {
      const result = await service.updateWorkflow(req.params.id, req.body);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.get('/api/workflow/:id', async (req: any, res: any) => {
    try {
      const result = await service.getWorkflow(req.params.id);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.get('/api/workflow/', async (req: any, res: any) => {
    try {
      const result = await service.getWorkflows();
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.get('/api/workflow/tasks/:id', async (req: any, res: any) => {
    try {
      const result = await service.getTasks(req.params.id);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.post('/api/workflow/tasks/:taskId/decision', async (req: any, res: any) => {
    try {
      const result = await service.decideTask(req.params.taskId, req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });
}
