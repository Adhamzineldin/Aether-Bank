// AUTO-GENERATED BY VELD — DO NOT EDIT
import type { ITransactionService } from '../interfaces/ITransactionService';
import { ApiError } from '../errors/transaction.errors';

export function transactionRouter(router: any, service: ITransactionService): void {

  router.post('/api/transaction_service/transactions/transfer', async (req: any, res: any) => {
    try {
      const result = await service.transfer(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.get('/api/transaction_service/transactions/account/:acountId', async (req: any, res: any) => {
    try {
      const result = await service.getAccountTransactions(req.params.acountId, req.body);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });
}
