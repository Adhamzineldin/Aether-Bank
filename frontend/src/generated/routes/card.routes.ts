// AUTO-GENERATED BY VELD — DO NOT EDIT
import type { ICardService } from '../interfaces/ICardService';
import { ApiError } from '../errors/card.errors';

export function cardRouter(router: any, service: ICardService): void {

  router.get('/api/card/:cardId', async (req: any, res: any) => {
    try {
      const result = await service.getCardDetails(req.params.cardId);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.get('/api/card/:cardId/pan', async (req: any, res: any) => {
    try {
      const result = await service.revealPan(req.params.cardId);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.post('/api/card/payments', async (req: any, res: any) => {
    try {
      const result = await service.processMerchantPayment(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.post('/api/card/refunds', async (req: any, res: any) => {
    try {
      const result = await service.refundTransaction(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.post('/api/card/voids', async (req: any, res: any) => {
    try {
      const result = await service.voidTransaction(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.get('/api/card/:cardId/transactions', async (req: any, res: any) => {
    try {
      const result = await service.getCardTransactions(req.params.cardId, req.body);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });
}
