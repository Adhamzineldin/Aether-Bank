// AUTO-GENERATED BY VELD — DO NOT EDIT
import type { IcertificateService } from '../interfaces/IcertificateService';
import { ApiError } from '../errors/certificate.errors';

export function certificateRouter(router: any, service: IcertificateService): void {

  // Submit a certificate application

  router.post('/api/financial_service/certificates/application/submit', async (req: any, res: any) => {
    try {
      const result = await service.certificateSubmit(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });
}
