// AUTO-GENERATED BY VELD — DO NOT EDIT
import type { IloanService } from '../interfaces/IloanService';
import { ApiError } from '../errors/loan.errors';

export function loanRouter(router: any, service: IloanService): void {

  // Submit a loan application

  router.post('/api/financial_service/api/loan/application/submit', async (req: any, res: any) => {
    try {
      const result = await service.loanSubmit(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });
}
