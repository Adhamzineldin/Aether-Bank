// AUTO-GENERATED BY VELD — DO NOT EDIT
import type { INotificationService } from '../interfaces/INotificationService';
import { ApiError } from '../errors/notification.errors';

export function notificationRouter(router: any, service: INotificationService): void {

  router.get('/api/notification/', async (req: any, res: any) => {
    try {
      const result = await service.listNotifications();
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.get('/api/notification/:id', async (req: any, res: any) => {
    try {
      const result = await service.getNotification(req.params.id);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.post('/api/notification/send', async (req: any, res: any) => {
    try {
      const result = await service.sendNotification(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.post('/api/notification/:id/retry', async (req: any, res: any) => {
    try {
      const result = await service.retryNotification(req.params.id);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.get('/api/notification/templates', async (req: any, res: any) => {
    try {
      const result = await service.listTemplates();
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.get('/api/notification/templates/:id', async (req: any, res: any) => {
    try {
      const result = await service.getTemplate(req.params.id);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.post('/api/notification/templates', async (req: any, res: any) => {
    try {
      const result = await service.createTemplate(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.put('/api/notification/templates/:id', async (req: any, res: any) => {
    try {
      const result = await service.updateTemplate(req.params.id, req.body);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.post('/api/notification/templates/:id/deactivate', async (req: any, res: any) => {
    try {
      const result = await service.deactivateTemplate(req.params.id);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });
}
