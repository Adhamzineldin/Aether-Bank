// AUTO-GENERATED BY VELD — DO NOT EDIT
import type { IAuthenticationService } from '../interfaces/IAuthenticationService';
import { ApiError } from '../errors/authentication.errors';

export function authenticationRouter(router: any, service: IAuthenticationService): void {

  router.post('/api/auth/login', async (req: any, res: any) => {
    try {
      const result = await service.login(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.post('/api/auth/register', async (req: any, res: any) => {
    try {
      const result = await service.register(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  router.post('/api/auth/logout', async (req: any, res: any) => {
    try {
      const result = await service.logout();
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });
}
