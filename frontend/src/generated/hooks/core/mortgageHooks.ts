// AUTO-GENERATED BY VELD — DO NOT EDIT
// Zero-dependency hooks — no TanStack Query required.
import { useState, useEffect, useCallback } from 'react';
import { mortgageApi } from '../../client/mortgageApi';
import type { MortgageApplication, MortgageApplicationResponse, MissingField, InvalidAmount, InvalidCreditScore, Mortgage, MortgageNotFound, CustomerNotFound, MortgageRepaymentSchedule, EmploymentStatus, ApplicationStatus, MortgageStatus, InstallmentStatus } from '../../types';

/** Submit a new mortgage application */
export function usesubmitMortgageApplication() {
  const [data, setData] = useState<MortgageApplicationResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: MortgageApplication }) => {
    setLoading(true);
    setError(null);
    return mortgageApi.submitMortgageApplication(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/** Get mortgage details by ID */
export function usegetMortgage(mortgageId: string) {
  const [data, setData] = useState<Mortgage | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    mortgageApi.getMortgage(mortgageId)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [mortgageId]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/** List all mortgages for a customer */
export function uselistCustomerMortgages(customerId: string) {
  const [data, setData] = useState<Mortgage[] | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    mortgageApi.listCustomerMortgages(customerId)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [customerId]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/** Get repayment schedule for a mortgage */
export function usegetMortgageSchedule(mortgageId: string) {
  const [data, setData] = useState<MortgageRepaymentSchedule[] | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    mortgageApi.getMortgageSchedule(mortgageId)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [mortgageId]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

export const mortgageHooks = {
  usesubmitMortgageApplication,
  usegetMortgage,
  uselistCustomerMortgages,
  usegetMortgageSchedule,
};
