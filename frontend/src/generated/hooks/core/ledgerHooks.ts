// AUTO-GENERATED BY VELD — DO NOT EDIT
// Zero-dependency hooks — no TanStack Query required.
import { useState, useEffect, useCallback } from 'react';
import { ledgerApi } from '../../client/ledgerApi';
import type { BalanceResponse, AccountLedgerNotInitialized } from '../../types';

/**  */
export function usegetAccountBalance(accountId: string, currency: string) {
  const [data, setData] = useState<BalanceResponse | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    ledgerApi.getAccountBalance(accountId, currency)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [accountId, currency]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

export const ledgerHooks = {
  usegetAccountBalance,
};
