// AUTO-GENERATED BY VELD — DO NOT EDIT
// Zero-dependency hooks — no TanStack Query required.
import { useState, useEffect, useCallback } from 'react';
import { loanApi } from '../../client/loanApi';
import type { LoanApplication, LoanApplicationResponse, MissingField, InvalidAmount, LoanType, EmploymentStatus, ApplicationStatus } from '../../types';

/** Submit a loan application */
export function useLoanSubmit() {
  const [data, setData] = useState<LoanApplicationResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: LoanApplication }) => {
    setLoading(true);
    setError(null);
    return loanApi.loanSubmit(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

export const loanHooks = {
  useLoanSubmit,
};
