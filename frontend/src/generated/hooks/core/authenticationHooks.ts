// AUTO-GENERATED BY VELD — DO NOT EDIT
// Zero-dependency hooks — no TanStack Query required.
import { useState, useEffect, useCallback } from 'react';
import { authenticationApi } from '../../client/authenticationApi';
import type { LoginRequest, JwtResponse, UserNotFound, InvalidCredentials, RegisterRequest, UserResponse, GenericResponse } from '../../types';

/**  */
export function uselogin() {
  const [data, setData] = useState<JwtResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: LoginRequest }) => {
    setLoading(true);
    setError(null);
    return authenticationApi.login(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/**  */
export function useregister() {
  const [data, setData] = useState<UserResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: RegisterRequest }) => {
    setLoading(true);
    setError(null);
    return authenticationApi.register(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/**  */
export function uselogout() {
  const [data, setData] = useState<GenericResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback(() => {
    setLoading(true);
    setError(null);
    return authenticationApi.logout()
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

export const authenticationHooks = {
  uselogin,
  useregister,
  uselogout,
};
