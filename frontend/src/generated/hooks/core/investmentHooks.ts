// AUTO-GENERATED BY VELD — DO NOT EDIT
// Zero-dependency hooks — no TanStack Query required.
import { useState, useEffect, useCallback } from 'react';
import { investmentApi } from '../../client/investmentApi';
import type { OpenInvestmentAccountRequest, InvestmentAccountResponse, CustomerNotFound, AccountNotFound, InvalidCurrency, BuyAssetRequest, TransactionResponse, AssetNotFound, InsufficientFunds, InvalidQuantity, MarketClosed, SellAssetRequest, InsufficientHolding, GetPortfolioRequest, Portfolio, GetPerformanceRequest, PerformanceMetrics, Asset, InvestmentAccount, InvestmentStatus, AssetType, AssetHolding, InvestmentTransaction, TransactionStatus } from '../../types';

/** Open a new investment account */
export function useopenInvestmentAccount() {
  const [data, setData] = useState<InvestmentAccountResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: OpenInvestmentAccountRequest }) => {
    setLoading(true);
    setError(null);
    return investmentApi.openInvestmentAccount(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/** Get investment account details */
export function usegetInvestmentAccount(accountId: string) {
  const [data, setData] = useState<InvestmentAccountResponse | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    investmentApi.getInvestmentAccount(accountId)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [accountId]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/** Purchase an investment asset */
export function usebuyAsset() {
  const [data, setData] = useState<TransactionResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { accountId: string; input: BuyAssetRequest }) => {
    setLoading(true);
    setError(null);
    return investmentApi.buyAsset(vars.accountId, vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/** Sell an investment asset */
export function usesellAsset() {
  const [data, setData] = useState<TransactionResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { accountId: string; input: SellAssetRequest }) => {
    setLoading(true);
    setError(null);
    return investmentApi.sellAsset(vars.accountId, vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/** Get portfolio holdings and valuation */
export function usegetPortfolio(accountId: string) {
  const [data, setData] = useState<Portfolio | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    investmentApi.getPortfolio(accountId)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [accountId]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/** Get investment performance metrics */
export function usegetPerformance(accountId: string) {
  const [data, setData] = useState<PerformanceMetrics | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    investmentApi.getPerformance(accountId)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [accountId]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/** List available assets for trading */
export function uselistAssets() {
  const [data, setData] = useState<Asset[] | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    investmentApi.listAssets()
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  }, []);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/** Get asset details and current price */
export function usegetAsset(symbol: string) {
  const [data, setData] = useState<Asset | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    investmentApi.getAsset(symbol)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [symbol]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

export const investmentHooks = {
  useopenInvestmentAccount,
  usegetInvestmentAccount,
  usebuyAsset,
  usesellAsset,
  usegetPortfolio,
  usegetPerformance,
  uselistAssets,
  usegetAsset,
};
