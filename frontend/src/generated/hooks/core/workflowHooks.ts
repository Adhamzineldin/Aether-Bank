// AUTO-GENERATED BY VELD — DO NOT EDIT
// Zero-dependency hooks — no TanStack Query required.
import { useState, useEffect, useCallback } from 'react';
import { workflowApi } from '../../client/workflowApi';
import type { CreateWorkflowInput, WorkflowInstance, UpdateWorkflowInput, ApprovalTask, TaskActionInput, WorkflowStatus, StepRole, TaskStatus, DecisionStatus, TaskDecision } from '../../types';

/**  */
export function useCreateWorkflow() {
  const [data, setData] = useState<WorkflowInstance | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: CreateWorkflowInput }) => {
    setLoading(true);
    setError(null);
    return workflowApi.createWorkflow(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/**  */
export function useUpdateWorkflow() {
  const [data, setData] = useState<WorkflowInstance | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { id: string; input: UpdateWorkflowInput }) => {
    setLoading(true);
    setError(null);
    return workflowApi.updateWorkflow(vars.id, vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/**  */
export function useGetWorkflow(id: string) {
  const [data, setData] = useState<WorkflowInstance | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    workflowApi.getWorkflow(id)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [id]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/**  */
export function useGetWorkflows() {
  const [data, setData] = useState<WorkflowInstance[] | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    workflowApi.getWorkflows()
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  }, []);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/**  */
export function useGetTasks(id: string) {
  const [data, setData] = useState<ApprovalTask[] | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    workflowApi.getTasks(id)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [id]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/**  */
export function useDecideTask() {
  const [data, setData] = useState<ApprovalTask | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { taskId: string; input: TaskActionInput }) => {
    setLoading(true);
    setError(null);
    return workflowApi.decideTask(vars.taskId, vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

export const workflowHooks = {
  useCreateWorkflow,
  useUpdateWorkflow,
  useGetWorkflow,
  useGetWorkflows,
  useGetTasks,
  useDecideTask,
};
