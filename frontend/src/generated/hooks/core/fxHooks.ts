// AUTO-GENERATED BY VELD — DO NOT EDIT
// Zero-dependency hooks — no TanStack Query required.
import { useState, useEffect, useCallback } from 'react';
import { fxApi } from '../../client/fxApi';
import type { GetRatesRequest, GetRatesResponse, InvalidCurrencyPair } from '../../types';

/**  */
export function usegetExchangeRate() {
  const [data, setData] = useState<GetRatesResponse | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    fxApi.getExchangeRate()
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  }, []);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

export const fxHooks = {
  usegetExchangeRate,
};
