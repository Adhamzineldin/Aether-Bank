// AUTO-GENERATED BY VELD — DO NOT EDIT
// Zero-dependency hooks — no TanStack Query required.
import { useState, useEffect, useCallback } from 'react';
import { transactionApi } from '../../client/transactionApi';
import type { TransferRequest, TransactionResponse, InsufficientFunds, InvalidAccount, Unauthorized, InvalidAmount, invalidTarget, getAccountTransactionsRequest, PaginatedTransactionResponse, AssetHolding, AssetType, InvestmentTransaction, TransactionStatus, TransactionType } from '../../types';

/**  */
export function usetransfer() {
  const [data, setData] = useState<TransactionResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: TransferRequest }) => {
    setLoading(true);
    setError(null);
    return transactionApi.transfer(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/**  */
export function usegetAccountTransactions(acountId: string) {
  const [data, setData] = useState<PaginatedTransactionResponse | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    transactionApi.getAccountTransactions(acountId)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [acountId]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

export const transactionHooks = {
  usetransfer,
  usegetAccountTransactions,
};
