// AUTO-GENERATED BY VELD — DO NOT EDIT
// Zero-dependency hooks — no TanStack Query required.
import { useState, useEffect, useCallback } from 'react';
import { cardApi } from '../../client/cardApi';
import type { CardDetailsResponse, NotFound, Unauthorized, PanRevealResponse, MerchantPaymentRequest, CardTransactionResponse, InvalidCardToken, CardNotActive, CardExpired, InsufficientCredit, InvalidCurrency, InvalidAmount, RefundCardTransactionRequest, TransactionNotFound, RefundNotAllowed, VoidCardTransactionRequest, VoidNotAllowed, GetCardTransactionsRequest, PaginatedCardTransactionResponse, CardSummary, CardType, CardNetwork, CardStatus, CreditCardDetails, CardTransactionStatus, CardTransactionType } from '../../types';

/**  */
export function usegetCardDetails(cardId: string) {
  const [data, setData] = useState<CardDetailsResponse | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    cardApi.getCardDetails(cardId)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [cardId]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/**  */
export function userevealPan(cardId: string) {
  const [data, setData] = useState<PanRevealResponse | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    cardApi.revealPan(cardId)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [cardId]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/**  */
export function useprocessMerchantPayment() {
  const [data, setData] = useState<CardTransactionResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: MerchantPaymentRequest }) => {
    setLoading(true);
    setError(null);
    return cardApi.processMerchantPayment(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/**  */
export function userefundTransaction() {
  const [data, setData] = useState<CardTransactionResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: RefundCardTransactionRequest }) => {
    setLoading(true);
    setError(null);
    return cardApi.refundTransaction(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/**  */
export function usevoidTransaction() {
  const [data, setData] = useState<CardTransactionResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: VoidCardTransactionRequest }) => {
    setLoading(true);
    setError(null);
    return cardApi.voidTransaction(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/**  */
export function usegetCardTransactions(cardId: string) {
  const [data, setData] = useState<PaginatedCardTransactionResponse | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    cardApi.getCardTransactions(cardId)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [cardId]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

export const cardHooks = {
  usegetCardDetails,
  userevealPan,
  useprocessMerchantPayment,
  userefundTransaction,
  usevoidTransaction,
  usegetCardTransactions,
};
