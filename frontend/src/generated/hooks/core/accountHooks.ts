// AUTO-GENERATED BY VELD — DO NOT EDIT
// Zero-dependency hooks — no TanStack Query required.
import { useState, useEffect, useCallback } from 'react';
import { accountApi } from '../../client/accountApi';
import type { OpenAccountRequest, AccountResponse, CustomerNotFound, InvalidCurrency, InvalidAccountType, AccountNotFound, CloseAccountRequest, AccountHasBalance, AccountAlreadyClosed, UpdateAccountStatusRequest, InvalidStatusTransition, BankAccount, AccountType, AccountStatus } from '../../types';

/** Open a new bank account */
export function useopenAccount() {
  const [data, setData] = useState<AccountResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: OpenAccountRequest }) => {
    setLoading(true);
    setError(null);
    return accountApi.openAccount(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/** Get account details with balance */
export function usegetAccount(accountId: string) {
  const [data, setData] = useState<AccountResponse | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    accountApi.getAccount(accountId)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [accountId]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/** List all accounts for a customer */
export function uselistCustomerAccounts(customerId: string) {
  const [data, setData] = useState<AccountResponse[] | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    accountApi.listCustomerAccounts(customerId)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [customerId]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/** Close an account */
export function usecloseAccount() {
  const [data, setData] = useState<AccountResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { accountId: string; input: CloseAccountRequest }) => {
    setLoading(true);
    setError(null);
    return accountApi.closeAccount(vars.accountId, vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/** Update account status (freeze/activate) */
export function useupdateAccountStatus() {
  const [data, setData] = useState<AccountResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { accountId: string; input: UpdateAccountStatusRequest }) => {
    setLoading(true);
    setError(null);
    return accountApi.updateAccountStatus(vars.accountId, vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/** Check if account exists */
export function usedoesAccountExist(accountId: string) {
  const [data, setData] = useState<boolean | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    accountApi.doesAccountExist(accountId)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [accountId]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

export const accountHooks = {
  useopenAccount,
  usegetAccount,
  uselistCustomerAccounts,
  usecloseAccount,
  useupdateAccountStatus,
  usedoesAccountExist,
};
