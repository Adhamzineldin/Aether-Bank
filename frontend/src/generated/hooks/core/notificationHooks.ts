// AUTO-GENERATED BY VELD — DO NOT EDIT
// Zero-dependency hooks — no TanStack Query required.
import { useState, useEffect, useCallback } from 'react';
import { notificationApi } from '../../client/notificationApi';
import type { NotificationItem, NotFound, Forbidden, SendNotificationInput, BadRequest, NotificationTemplate, CreateTemplateInput, UpdateTemplateInput, NotificationEventType, NotificationChannel, NotificationType, NotificationStatus } from '../../types';

/**  */
export function useListNotifications() {
  const [data, setData] = useState<NotificationItem[] | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    notificationApi.listNotifications()
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  }, []);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/**  */
export function useGetNotification(id: string) {
  const [data, setData] = useState<NotificationItem | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    notificationApi.getNotification(id)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [id]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/**  */
export function useSendNotification() {
  const [data, setData] = useState<NotificationItem | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: SendNotificationInput }) => {
    setLoading(true);
    setError(null);
    return notificationApi.sendNotification(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/**  */
export function useRetryNotification() {
  const [data, setData] = useState<NotificationItem | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { id: string }) => {
    setLoading(true);
    setError(null);
    return notificationApi.retryNotification(vars.id)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/**  */
export function useListTemplates() {
  const [data, setData] = useState<NotificationTemplate[] | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    notificationApi.listTemplates()
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  }, []);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/**  */
export function useGetTemplate(id: string) {
  const [data, setData] = useState<NotificationTemplate | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    notificationApi.getTemplate(id)
      .then((result) => { setData(result); })
      .catch((err) => { setError(err); })
      .finally(() => { setLoading(false); });
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [id]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/**  */
export function useCreateTemplate() {
  const [data, setData] = useState<NotificationTemplate | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: CreateTemplateInput }) => {
    setLoading(true);
    setError(null);
    return notificationApi.createTemplate(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/**  */
export function useUpdateTemplate() {
  const [data, setData] = useState<NotificationTemplate | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { id: string; input: UpdateTemplateInput }) => {
    setLoading(true);
    setError(null);
    return notificationApi.updateTemplate(vars.id, vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

/**  */
export function useDeactivateTemplate() {
  const [data, setData] = useState<NotificationTemplate | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { id: string }) => {
    setLoading(true);
    setError(null);
    return notificationApi.deactivateTemplate(vars.id)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

export const notificationHooks = {
  useListNotifications,
  useGetNotification,
  useSendNotification,
  useRetryNotification,
  useListTemplates,
  useGetTemplate,
  useCreateTemplate,
  useUpdateTemplate,
  useDeactivateTemplate,
};
