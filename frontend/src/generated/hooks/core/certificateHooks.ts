// AUTO-GENERATED BY VELD — DO NOT EDIT
// Zero-dependency hooks — no TanStack Query required.
import { useState, useEffect, useCallback } from 'react';
import { certificateApi } from '../../client/certificateApi';
import type { CertificateApplication, CertificateApplicationResponse, MissingField, InvalidAmount, ApplicationStatus } from '../../types';

/** Submit a certificate application */
export function useCertificateSubmit() {
  const [data, setData] = useState<CertificateApplicationResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: CertificateApplication }) => {
    setLoading(true);
    setError(null);
    return certificateApi.certificateSubmit(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => { setLoading(false); });
  }, []);

  return { mutate, data, loading, error };
}

export const certificateHooks = {
  useCertificateSubmit,
};
